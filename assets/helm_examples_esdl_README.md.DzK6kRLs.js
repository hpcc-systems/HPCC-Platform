import{_ as a,a as s,o as t,ag as l}from"./chunks/framework.Do1Zayaf.js";const u=JSON.parse('{"title":"ESDL Service Deployment","description":"","frontmatter":{},"headers":[],"relativePath":"helm/examples/esdl/README.md","filePath":"helm/examples/esdl/README.md","lastUpdated":1770295448000}'),o={name:"helm/examples/esdl/README.md"};function i(n,e,c,d,p,h){return t(),s("div",null,e[0]||(e[0]=[l(`<h1 id="esdl-service-deployment" tabindex="-1">ESDL Service Deployment <a class="header-anchor" href="#esdl-service-deployment" aria-label="Permalink to &quot;ESDL Service Deployment&quot;">​</a></h1><p>This directory has files and directions showing how you can include an esdl application service as part of an HPCCSystems platform helm install. This is distinguished from an esdl-sandbox application in that it loads its service bundle from a file rather than from dali.</p><p>The primary consideration for this example is the location of the bundle file. When running the helm install, the values you supply must include the path to the bundle file(s) in the <code>esp.bindings</code> key. The two possibilites we cover here are just suggestions- you can store your bundle anywhere you like, as long as you supply the correct path in your values file.</p><p>For each case below, you can test the running esdl service by navigating to <code>http://localhost:7770</code> in your browser. You will see a simple service, WsEchoAddress, that will echo back an address provided on input.</p><h2 id="bundle-in-the-docker-image" tabindex="-1">Bundle in the docker image <a class="header-anchor" href="#bundle-in-the-docker-image" aria-label="Permalink to &quot;Bundle in the docker image&quot;">​</a></h2><p>In this case you will build a docker image based on a platform image and copy in the bundle file. Because the helm layer can overwrite what is in the running container, be sure not to use a path that helm mounts a volume to, such as <code>/etc/config</code> or <code>/var/lib/HPCCSystems</code>. For that reason we use <code>/home/hpcc/esdl</code>.</p><p>First we build the image, keeping it in the local library, not pushing to any repo:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>cd helm/examples/esdl/image</span></span>
<span class="line"><span>docker build -t esdl-echoapp .</span></span></code></pre></div><p>Now install the helm chart, using the image we just built and our custom values for the esdl service:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>helm install mycluster hpcc/hpcc -f helm/examples/esdl/values-echo-example-image.yaml --set global.image.version=latest --set global.image.root=library --set global.image.name=esdl-echoapp</span></span></code></pre></div><h2 id="bundle-on-a-persistent-volume" tabindex="-1">Bundle on a persistent volume <a class="header-anchor" href="#bundle-on-a-persistent-volume" aria-label="Permalink to &quot;Bundle on a persistent volume&quot;">​</a></h2><p>In this case the esdl bundle file is located on a persistent volume mounted to the esdl pod. We&#39;re extending the <code>local/hpcc-localfile</code> example for <em>Docker desktop (using wsl2 to access windows data)</em>, so familiarize yourself with that before continuing. The volume mount path is <code>/var/lib/HPCCSystems/esdl</code>, with sub-directories named for each ESP instance which holds that ESP&#39;s esdl bundles. The ESP instance name in this example is <code>echo-example</code></p><p>Some setup is required on your local machine prior to installing the helm charts:</p><ol><li>Create the host directory <code>C:\\hpccdata</code></li><li>Create subdirectories named <code>dalistorage</code>, <code>debug</code>, <code>dropzone</code>, <code>esdl\\echo-example</code>, <code>hpcc-data</code>, <code>queries</code> and <code>sasha</code>.</li><li>Copy <code>helm/examples/esdl/image/echo_address-bundle.xml</code> to <code>C:\\hpccdata\\esdl\\echo-example</code> so ESP can load it on startup. The ESP does not re-attempt to load bundles after its initial startup.</li></ol><p>Next install the <code>hpcc-localfile</code> chart with some custom values:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>helm install localfile helm/examples/local/hpcc-localfile -f helm/examples/esdl/values-hpcc-localfile.yaml --set common.hostpath=/run/desktop/mnt/host/c/hpccdata</span></span></code></pre></div><p>Then install the hpcc cluster using custom values to mount our local volumes and run just our <code>echo-example</code> esdl application ESP:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>helm install mycluster hpcc/hpcc --set global.image.version=latest -f helm/examples/esdl/values-echo-example-localfile.yaml</span></span></code></pre></div>`,18)]))}const m=a(o,[["render",i]]);export{u as __pageData,m as default};
