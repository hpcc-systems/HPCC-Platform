import{_ as t,a as o,o as a,ag as n}from"./chunks/framework.Do1Zayaf.js";const p=JSON.parse('{"title":"Modular Log Agent Plugin","description":"","frontmatter":{},"headers":[],"relativePath":"esp/logging/loggingagent/modularlogagent/README.md","filePath":"esp/logging/loggingagent/modularlogagent/README.md","lastUpdated":1770295448000}'),i={name:"esp/logging/loggingagent/modularlogagent/README.md"};function r(s,e,d,l,u,g){return a(),o("div",null,e[0]||(e[0]=[n(`<h1 id="modular-log-agent-plugin" tabindex="-1">Modular Log Agent Plugin <a class="header-anchor" href="#modular-log-agent-plugin" aria-label="Permalink to &quot;Modular Log Agent Plugin&quot;">​</a></h1><h2 id="about" tabindex="-1">About <a class="header-anchor" href="#about" aria-label="Permalink to &quot;About&quot;">​</a></h2><p>A single log agent plugin can be used to create a single log agent implementation. Support for multiple types of logging (e.g., writing to a file versus updating a database), requires either a plugin for each type or a monolithic agent that tries to do everything. In an effort to reduce the proliferation of plugins, this plugin uses a new monolithic log agent modular framework. Rather than expecting a single class to do everything, the framework breaks down agent functionality into a set of small and possibly reusable components referred to as modules. Access to customer-agnostic implementations of functionality is provided through this plugin. Access to customer-centric implementations should not be included in this plugin; one or more proprietary plugins should be developed to address those requirements.</p><p>A logging manager finds occurrences of <strong>LogAgent</strong> within its configuration. Each instance includes a number of properties which can be separated into two categories. Properties defined and used by this agent&#39;s framework, i.e., properties that distinguish this agent from others, are referred to as <em>modular</em> properties. Properties common to many agents and used by the broader logging engine are referred to as <em>agent integration</em> properties.</p><p>This document describes how to configure an instance of the plugin for use within the overall logging framework. The intended audience is configuration authors, i.e., individuals responsible for creating configurations using a deployed plugin as opposed to developers maintaining or extending the plugin. Because no previous plugin has required a manual configuration, as this does, limited attention will be given to <em>agent integration</em> properties, but the document&#39;s focus is on the definition and use of <em>modular</em> properties.</p><p>A convention used in the document is that within each section that describes a module, one or more subsections are included that contain quoted text in the section heading. These quoted text strings are the plugin-defined selectors used by the plugin to create the configured module implementations. Each module configuration includes an optional <em>module</em> attribute that is used to select an implementation. If the attribute is omitted or empty, the <em>default</em> implementation is selected. A configuration requesting any <em>module</em> value other than those described here is invalid.</p><h3 id="modules" tabindex="-1">Modules <a class="header-anchor" href="#modules" aria-label="Permalink to &quot;Modules&quot;">​</a></h3><h4 id="logagent" tabindex="-1">LogAgent <a class="header-anchor" href="#logagent" aria-label="Permalink to &quot;LogAgent&quot;">​</a></h4><p>The only module that is required to use the plugin is <strong>LogAgent</strong>. This module provides access to all log agent service functionality. Implementations may be monolithic, including all agent functionality within one class. Alternate implementations may themselves be modular, relying upon smaller, more focused service modules to provide interchangeable functionality.</p><p>Monolithic implementations are permitted when they provide the best solution for logging requirements. One plugin may still be configured for multiple types of logging. Modular implementations are preferred due to increased flexibility and reuse.</p><h5 id="default" tabindex="-1">&quot;default&quot; <a class="header-anchor" href="#default" aria-label="Permalink to &quot;&quot;default&quot;&quot;">​</a></h5><p>The default implementation relies upon separate service module implementations for each supported agent service. The use of service modules increases plugin flexibility, especially as the list of supported services expands.</p><p>Configuring a supported service module implicitly enables that service. Alternatively, to support YAML property files, a default service module implementation may be requested with a Boolean property in place of an empty element. Omission of a service module configuration explicitly disables that service.</p><p>Of the defined log agent services, only one - UpdateLog - is supported. Support for the remaining services will be added in future updates.</p><h5 id="mock" tabindex="-1">&quot;mock&quot; <a class="header-anchor" href="#mock" aria-label="Permalink to &quot;&quot;mock&quot;&quot;">​</a></h5><p>A mocked agent can be configured to produce any response that another agent may produce, with no depenencies on input or external sources. Each service for which the configuration defines a response is enabled. Each service for which the configuration does not define a response is disabled.</p><p>Although intended primarily for use with unit tests, exposing the module through the plugin causes no harm and may provide value for testing that is not part of the unit tests.</p><h4 id="updatelog" tabindex="-1">UpdateLog <a class="header-anchor" href="#updatelog" aria-label="Permalink to &quot;UpdateLog&quot;">​</a></h4><p>Implementations of the <strong>UpdateLog</strong> service module provide log update request processing. Implementations may be monolithic, handling update requests from end to end. Alternate implementations may themselves be modular, separating generic functionality from implementation details associated with various forms of persistence (e.g., database, file, or web service updates).</p><p>The input to this module will be log content data supplied to the log agent by the log thread. All data transformations may assume this as a starting point. However, the existence of content filters in the manager and agent configurations, as well as custom transformations in the transactional ESP, may radically alter the input. It is the configuration author&#39;s responsibility to ensure that meets expectations.</p><h5 id="default-1" tabindex="-1">&quot;default&quot; <a class="header-anchor" href="#default-1" aria-label="Permalink to &quot;&quot;default&quot;&quot;">​</a></h5><p>The default implementation relies upon separate <strong>Target</strong> delegate module implementations to persist data prepared by the service module. This creates a logical distinction between the preparation of data to be persisted, which is the responsibility of this module, and the persistence of data, which is the responsibility of <strong>Target</strong> modules. It separates the generic manipulation of update request data from the details of persisting the data.</p><p>Each target has expectations of the data provided by the service module. It is the configuration author&#39;s responsibility to ensure that the data prepared by the configured service module meets the requirements of the configured delegate module.</p><p>Configuring a delegate module implicitly enables data persistence. Omission of a target module configuration limits data persistence to conditional inclusion in trace log output.</p><h4 id="target" tabindex="-1">Target <a class="header-anchor" href="#target" aria-label="Permalink to &quot;Target&quot;">​</a></h4><p>Implementations of the <strong>Target</strong> delegate module interface persist data supplied by an <strong>UpdateLog</strong> service module. Three data inputs may be provided to each instance, including the complete update request (the <em>original</em> input), the possible output of custom transformations applied to the complete update request (the <em>intermediate</em> input), and the possible output of an XSL transformation applied to either of the first two inputs (the <em>final</em> input). Each implementation defines which inputs it uses and how it uses them. It is the configuration author&#39;s responsibility to ensure that the <strong>UpdateLog</strong> module produces the inputs required by the implementation.</p><h5 id="default-2" tabindex="-1">&quot;default&quot; <a class="header-anchor" href="#default-2" aria-label="Permalink to &quot;&quot;default&quot;&quot;">​</a></h5><p>The default implementation accepts all content and conditionally adds it to trace log output. This functionality is provided as a development and testing aid.</p><h5 id="file" tabindex="-1">&quot;file&quot; <a class="header-anchor" href="#file" aria-label="Permalink to &quot;&quot;file&quot;&quot;">​</a></h5><p>File logging operates in either a debug or release mode, with the release mode enabled by default. In release mode, the implementation writes the <em>final</em> input into a file. In debug mode, the implementation writes all provided inputs into a file. In both modes, filepath variable values are obtained from the <em>original</em> input.</p><p>A small number of well-known values included in an update request may be referenced in the <em>filepath</em> property definition. Output files need to be differentiated by the ESP service instance that created the transaction instead of the ESP service instance processing the log update. The use of dynamic substitutions instead of hard coded configuration values improves the reusability of configurations. Refer to the <strong>Configuration Reference</strong> for details of which values are available.</p><p>It is possible, if not likely, that one instance of this module will be expected to manage content in multiple files. The use of variable substitutions in the filepath makes it conceivable that every transaction could be written to a new file. To avoid unconstrained use of system resources, a limit on the number of files a module instance has open at one time is imposed through the configuration. It should only be necessary to change the default value when more than one file is expected. For example, multiple files would be expected if the <em>filepath</em> property includes a reference to the ESDL method name.</p><p>To prevent output files from growing to a difficult to manage size, file rollover criteria can be given. Rollover on date change, a pattern seen elsewhere in the platform, is enabled by default. This may be insufficient for heavily used services, which is why rollover by file size may be enabled.</p><h2 id="usage" tabindex="-1">Usage <a class="header-anchor" href="#usage" aria-label="Permalink to &quot;Usage&quot;">​</a></h2><p>The modular log agent supports two configuration scenarios.</p><p>In the <em><strong>inline configuration</strong></em> scenario, an instance of <strong>LogAgent</strong> contains all available information for the agent. <em>Modular</em> and <em>agent integration</em> properties coexist in the same instance. Everything is in one place; the configuration is <em><strong>inline</strong></em>.</p><p>In the <em><strong>split configuration</strong></em> scenario, an instance of <strong>LogAgent</strong> contains the <em>agent integration</em> properties plus a reference to a separate file specifying <em>modular</em> properties. One configuration is derived from multiple sources; the configuration is <em><strong>split</strong></em>.</p><p>Logging configurations are presented to an ESP from multiple sources. Each configuration scenario is optimized for one or more of these sources, but may be applied to each source.</p><h3 id="legacy" tabindex="-1">Legacy <a class="header-anchor" href="#legacy" aria-label="Permalink to &quot;Legacy&quot;">​</a></h3><p>The ESP process may read an <em>esp.xml</em> configuration file to configure itself and its services. Traditionally this file is auto-generated from an <em>environment.xml</em> file created using <em>configmgr</em>. The ability for <em>configmgr</em> to configure constructs of which it is unaware is limited. Configurations specified with <em>configmgr</em> generally use a split configuration, but an inline configuration may be used to request default agent behavior.</p><p><em>Configmgr</em> includes a component named <em>modularlogagent</em>. This component is used to configure agents from any plugin using the modular agent framework. The modules, and associated <em>modular</em> properties, included in each plugin are unknowns. Reuse by multiple plugins requires that little to no <em>modular</em> property knowledge exists in the component. The component supports the specification of <em>agent integration</em> properties, a split configuration property file, and (to support the default inline configuration option) a trace logging limit.</p><table tabindex="0"><thead><tr><th>Property</th><th>Description</th></tr></thead><tbody><tr><td>configuration</td><td>Optional file path to either an XML or YAML file containing one <strong>LogAgent</strong> element. An empty value implies the use of the default inline configuration. A non-empty value implies the use of a split configuration with all <em>modular</em> properties defined in the external file.</td></tr><tr><td>plugin</td><td>Required shared library identifier. Defaulting to <em>modularlogagent</em>, this value may be changed to refer to another library (e.g., <em>scappsmodularlogagent</em> for this plugin).</td></tr><tr><td>trace-priority-level</td><td>Maximum trace log output message severity to be included in trace log output. The value is ignored unless <em>configuration</em> is empty, in which case the limit applies to the agent and all default modules.</td></tr></tbody></table><p>It is possible to start an ESP process with a manually edited <em>esp.xml</em> configuration file, and doing so can be useful for debugging. A split configuration could be replaced with an inline equivalent, but this should not be necessary and is discouraged.</p><h3 id="esp-as-an-application" tabindex="-1">ESP as an Application <a class="header-anchor" href="#esp-as-an-application" aria-label="Permalink to &quot;ESP as an Application&quot;">​</a></h3><p>The ESP process, rather than reading a traditional <em>esp.xml</em> file, may read an assortment of YAML property files and assemble a configuration equivalent to <em>esp.xml</em>. Because this assembly process will understand the relationship of any log agent to a manager, and a manager to an ESP, the assembled configuration could be an inline configuration.</p><p>This static binding configuration is meant to be limited to user values for essential process setup parameters. Logging configurations are not essential. An end user may consider logging essential to their deployment of an ESP, but the ESP itself is capable of operating without it. Inline configurations are supported for backward compatibility, but split configurations (or an as yet undefined third configuration scenario) may be preferred.</p><h3 id="dynamic-esdl-binding" tabindex="-1">Dynamic ESDL Binding <a class="header-anchor" href="#dynamic-esdl-binding" aria-label="Permalink to &quot;Dynamic ESDL Binding&quot;">​</a></h3><p>In addition to a logging manager configured by either <strong>Legacy</strong> or <strong>ESP as an Application</strong>, the dynamic ESDL service can use managers configured in the dynamic service bindings loaded at runtime. These configurations should be inline, as split configurations may introduce unwanted cross-dependencies between services.</p><p>At the time of this writing, the binding XML must be manually edited. Support for automated assembly of bindings from smaller, reusable property files may be provided in the future.</p><h2 id="sample-configurations" tabindex="-1">Sample Configurations <a class="header-anchor" href="#sample-configurations" aria-label="Permalink to &quot;Sample Configurations&quot;">​</a></h2><p>These examples show only mandatory <em>agent integration</em> and key <em>modular</em> properties. The illustration of optional <em>agent integration</em> properties is beyond the scope of this document.</p><p>Each example provides both XML and YAML sample markup for inline configurations. With limited exceptions, the samples represent external property files content used by split configurations. These exceptions are addressed on a case by case basis.</p><h3 id="default-no-logging-agent" tabindex="-1">Default &quot;no-logging&quot; Agent <a class="header-anchor" href="#default-no-logging-agent" aria-label="Permalink to &quot;Default &quot;no-logging&quot; Agent&quot;">​</a></h3><p>Conditionally append log update data to trace log output. The condition to ensure output is an ESP process <em>logLevel</em> property value of at least <em>9</em>.</p><h4 id="xml" tabindex="-1">XML <a class="header-anchor" href="#xml" aria-label="Permalink to &quot;XML&quot;">​</a></h4><p>Either:</p><pre><code>&lt;LogAgent name=&quot;default-no-logging&quot; plugin=&quot;scappsmodularlogagent&quot; UpdateLog=&quot;true&quot;/&gt;
</code></pre><p>or:</p><pre><code>&lt;LogAgent name=&quot;default-no-logging&quot; plugin=&quot;scappsmodularlogagent&quot;&gt;
  &lt;UpdateLog/&gt;
&lt;/LogAgent&gt;
</code></pre><p>When originating from <em>configmgr</em>, if the <em>environment.xml</em> configuration omits both <em>configuration</em> and <em>trace-priority-level</em> properties, the second form is generated in <em>esp.xml</em>. No external property file is used.</p><h4 id="yaml" tabindex="-1">YAML <a class="header-anchor" href="#yaml" aria-label="Permalink to &quot;YAML&quot;">​</a></h4><pre><code>LogAgent:
  name: default-no-logging
  plugin: scappsmodularlogagent
  UpdateLog: true
</code></pre><h3 id="verbose-no-logging-agent" tabindex="-1">Verbose &quot;no-logging&quot; Agent <a class="header-anchor" href="#verbose-no-logging-agent" aria-label="Permalink to &quot;Verbose &quot;no-logging&quot; Agent&quot;">​</a></h3><p>Conditionally append log update data to trace log output. The condition to ensure output is a <em>trace-priority-limit</em> property value of at least <em>90</em>. Unlike in the <strong>Default &quot;no-logging&quot; Agent</strong> example, this agent controls its own trace output independent of the ESP process and any other configured agent.</p><h4 id="xml-1" tabindex="-1">XML <a class="header-anchor" href="#xml-1" aria-label="Permalink to &quot;XML&quot;">​</a></h4><p>Either:</p><pre><code>&lt;LogAgent name=&quot;verbose-no-logging&quot; plugin=&quot;scappsmodularlogagent&quot; trace-priority-limit=&quot;90&quot; UpdateLog=&quot;true&quot;/&gt;
</code></pre><p>Or:</p><pre><code>&lt;LogAgent name=&quot;verbose-no-logging&quot; plugin=&quot;scappsmodularlogagent&quot; trace-priority-limit=&quot;90&quot;&gt;
  &lt;UpdateLog/&gt;
&lt;/LogAgent&gt;
</code></pre><p>When originating from <em>configmgr</em>, if the <em>environment.xml</em> configuration omits the <em>configuration</em> property, the second form is generated in <em>esp.xml</em>. No external property file is used.</p><h4 id="yaml-1" tabindex="-1">YAML <a class="header-anchor" href="#yaml-1" aria-label="Permalink to &quot;YAML&quot;">​</a></h4><pre><code>LogAgent:
  name: verbose-no-logging
  plugin: scappsmodularlogagent
  trace-priority-limit: 90
  UpdateLog: true
</code></pre><h3 id="terse-no-logging-agent" tabindex="-1">Terse &quot;no-logging&quot; Agent <a class="header-anchor" href="#terse-no-logging-agent" aria-label="Permalink to &quot;Terse &quot;no-logging&quot; Agent&quot;">​</a></h3><p>Refine the output produced by the <strong>Verbose &quot;no-logging&quot; Agent</strong> example by quieting the <strong>LogAgent</strong> and <strong>UpdateLog</strong> modules, and only ensuring recording the <strong>Target</strong> module input. As the complexities of the default modules, especially <strong>UpdateLog</strong>, increases, this example could reduce potentially excessive trace output.</p><h4 id="xml-2" tabindex="-1">XML <a class="header-anchor" href="#xml-2" aria-label="Permalink to &quot;XML&quot;">​</a></h4><pre><code>&lt;LogAgent name=&quot;terse-no-logging&quot; plugin=&quot;scappsmodularlogagent&quot; trace-priority-limit=&quot;10&quot;&gt;
  &lt;UpdateLog&gt;
    &lt;Target trace-priority-limit=&quot;90&quot;/&gt;
  &lt;/UpdateLog&gt;
&lt;/LogAgent&gt;
</code></pre><h4 id="yaml-2" tabindex="-1">YAML <a class="header-anchor" href="#yaml-2" aria-label="Permalink to &quot;YAML&quot;">​</a></h4><pre><code>LogAgent:
  name: terse-no-logging
  plugin: scappsmodularlogagent
  trace-priority-limit: 10
  UpdateLog:
    Target:
      trace-priority-limit: 90
</code></pre><h3 id="disabled-updatelog" tabindex="-1">Disabled UpdateLog <a class="header-anchor" href="#disabled-updatelog" aria-label="Permalink to &quot;Disabled UpdateLog&quot;">​</a></h3><p>Assuming a fully configured agent including an explicit <strong>UpdateLog</strong> specification, it may be necessary to temporarily prevent the agent from processing update requests. Removing <strong>UpdateLog</strong> would accomplish this, but it may be preferable to retain and disable it.</p><h4 id="xml-3" tabindex="-1">XML <a class="header-anchor" href="#xml-3" aria-label="Permalink to &quot;XML&quot;">​</a></h4><pre><code>&lt;LogAgent name=&quot;file-logging&quot; plugin=&quot;scappsmodularlogagent&quot;&gt;
  &lt;UpdateLog disabled=&quot;true&quot;&gt;
    &lt;Target module=&quot;file&quot; filepath=&quot;&lt;your-output-file&gt;&quot;/&gt;
  &lt;/UpdateLog&gt;
&lt;/LogAgent&gt;
</code></pre><h4 id="yaml-3" tabindex="-1">YAML <a class="header-anchor" href="#yaml-3" aria-label="Permalink to &quot;YAML&quot;">​</a></h4><pre><code>LogAgent:
  name: file-logging
  plugin: scappsmodularlogagent
  UpdateLog:
    disabled: true
    Target:
      module: file
      filepath: &lt;your-output-file&gt;
</code></pre><h3 id="disabled-target" tabindex="-1">Disabled Target <a class="header-anchor" href="#disabled-target" aria-label="Permalink to &quot;Disabled Target&quot;">​</a></h3><p>Assuming a fully configured agent including an explicit <strong>Target</strong> specification, it may be appropriate to allow <strong>UpdateLog</strong> to process a request while preventing <strong>Target</strong> from acting on the results. When more content processing exists in <strong>UpdateLog</strong> than in <strong>Target</strong>, validation of <strong>UpdateLog</strong> changes without persisting the results is a helpful debugging tool.</p><h4 id="xml-4" tabindex="-1">XML <a class="header-anchor" href="#xml-4" aria-label="Permalink to &quot;XML&quot;">​</a></h4><pre><code>&lt;LogAgent name=&quot;file-logging&quot; plugin=&quot;scappsmodularlogagent&quot;&gt;
  &lt;UpdateLog&gt;
    &lt;Target module=&quot;file&quot; filepath=&quot;&lt;your-output-file&gt;&quot; disabled=&quot;true&quot;/&gt;
  &lt;/UpdateLog&gt;
&lt;/LogAgent&gt;
</code></pre><h4 id="yaml-4" tabindex="-1">YAML <a class="header-anchor" href="#yaml-4" aria-label="Permalink to &quot;YAML&quot;">​</a></h4><pre><code>LogAgent:
  name: file-logging
  plugin: scappsmodularlogagent
  UpdateLog:
    Target:
      module: file
      filepath: &lt;your-output-file&gt;
      disabled: true
</code></pre><h3 id="generic-file-logging" tabindex="-1">Generic File Logging <a class="header-anchor" href="#generic-file-logging" aria-label="Permalink to &quot;Generic File Logging&quot;">​</a></h3><p>Write unprocessed log update data to a file. The use of the <em>debug</em> property is necessary to produce content, and will remain so until data transformations are implemented in <strong>UpdateLog</strong>.</p><h4 id="xml-5" tabindex="-1">XML <a class="header-anchor" href="#xml-5" aria-label="Permalink to &quot;XML&quot;">​</a></h4><pre><code>&lt;LogAgent name=&quot;file-logging&quot; plugin=&quot;scappsmodularlogagent&quot;&gt;
  &lt;UpdateLog&gt;
    &lt;Target module=&quot;file&quot; filepath=&quot;&lt;your-output-file&gt;&quot; debug=&quot;true&quot;/&gt;
  &lt;/UpdateLog&gt;
&lt;/LogAgent&gt;
</code></pre><h4 id="yaml-5" tabindex="-1">YAML <a class="header-anchor" href="#yaml-5" aria-label="Permalink to &quot;YAML&quot;">​</a></h4><pre><code>LogAgent:
  name: file-logging
  plugin: scappsmodularlogagent
  UpdateLog:
    Target:
      module: file
      filepath: &lt;your-output-file&gt;
      debug: true
</code></pre><h2 id="configuration-reference" tabindex="-1">Configuration Reference <a class="header-anchor" href="#configuration-reference" aria-label="Permalink to &quot;Configuration Reference&quot;">​</a></h2><p>All <em>agent integration</em> and <em>modular</em> properties may be defined using either XML or YAML markup. This reference uses XML and XPath terminology to describe the properties.</p><h3 id="loggingmanager-logagent" tabindex="-1">//LoggingManager/LogAgent <a class="header-anchor" href="#loggingmanager-logagent" aria-label="Permalink to &quot;//LoggingManager/LogAgent&quot;">​</a></h3><h4 id="agent-integration-properties" tabindex="-1"><em>Agent Integration</em> Properties <a class="header-anchor" href="#agent-integration-properties" aria-label="Permalink to &quot;*Agent Integration* Properties&quot;">​</a></h4><p>All <em>agent integration</em> properties are either properties or descendents of <strong>LogAgent</strong>. <em>Agent integration</em> properties do not overlap <em>modular</em> properties, with the exception of <em>LogAgent/@name</em>. An agent name is required in inline configurations and is optional in the external property file used with a split configuration.</p><table tabindex="0"><thead><tr><th>Key</th><th>Component</th><th>Usage</th></tr></thead><tbody><tr><td>M</td><td>Logging Manager</td><td>Reads the value, but does not require it.</td></tr><tr><td>m</td><td>Logging Manager</td><td>Requires the value.</td></tr><tr><td>T</td><td>Log Thread</td><td>Reads the value, but does not require it.</td></tr><tr><td>S</td><td>Log FailSafe</td><td>Reads the value, but does not require it; the utility is used by the log thread.</td></tr><tr><td>A</td><td>Log Agent</td><td>Reads the value, but does not require it.</td></tr><tr><td>a</td><td>Log Agent</td><td>Requires the value.</td></tr><tr><td>F</td><td>Log Content Filter</td><td>Reads the value, but does not require it; the utility is used by the log agent.</td></tr></tbody></table><p>The preceding table is a legend for values in the <em>Usage</em> column in the following table. Each key value indicates both the component that uses the value and whether the component requires a value to exist. A reader interested in creating configurations without understanding the implementation need only focus on the case of the key value - upper case identifies an optional value and lower case identifies a required value.</p><p>Some properties identify two XPaths, one an attribute and one an element. Historically, these properties were defined as elements. With this plugin, they may now be defined as attributes. XML configurations work well with either form, but element notation is not used in YAML configurations. Both forms are supported, with attributes taking precedence over elements.</p><table tabindex="0"><thead><tr><th>XPath</th><th>Usage</th><th>Description</th></tr></thead><tbody><tr><td>@name</td><td>m, a</td><td>Agent instance identifier required by the manager. It is used by multiple components for trace log output. Agent variant identification implicitly assumes the value is a unique identifier, based on bare-metal configurations generated using <em>configmgr</em>, but uniqueness is not enforced.</td></tr><tr><td>@plugin</td><td>m</td><td>Complete or base shared library name. If the base name is <em>modularlogagent</em>, the complete name might be <em>libmodularlogagent.so</em>.</td></tr><tr><td>@type</td><td>M</td><td>Secondary value used by some other agents for trace log output. Unused by the modular log agent.</td></tr><tr><td>@AckedFiles / <s>AckedFiles</s></td><td>T</td><td>Path to a file listing FailSafe log files that have been acknowledged by an agent instance. Omitted or empty defaults to <em>AckedFiles</em>.</td></tr><tr><td>@AckedLogRequests / <s>AckedLogRequests</s></td><td>T</td><td>Path to file listing the log update request GUIDs that have been acknowledged by an agent instance. Omitted or empty defaults to <em>AckedLogRequests</em>.</td></tr><tr><td>@DecoupledLogging / <s>DecoupledLogging</s></td><td>S</td><td>Boolean value indicating whether the agent uses decoupled logging (<em>true</em>) or not (<em>false</em>). This is a manager-specific property read by a utility class shared by the manager and thread. It <em>must not</em> be set in an agent configuration.</td></tr><tr><td>@DisableFailSafe / <s>DisableFailSafe</s></td><td>T</td><td>Boolean value indicating whether agent-specific FailSafe logging configurations should be ignored (<em>true</em>) or honored (<em>false</em>). It is meant to be <em>true</em> when the logging manager is managing FailSafe log files.</td></tr><tr><td>@FailSafe / <s>FailSafe</s></td><td>T</td><td>Boolean value indicating whether the agent requests per-agent FailSafe log files (<em>true</em>) or not (<em>false</em>).</td></tr><tr><td>@FailSafeLogsDir / <s>FailSafeLogsDir</s></td><td>S</td><td>Directory in which FailSafe log files are created. Omitted or empty defaults to <em>./FailSafeLogs</em>.</td></tr><tr><td>Filters/Filter</td><td>F</td><td>Repeating element used to prune the log request XML document. Each element may specify an XPath into the complete document, or an XPath into a specific section of a standard <em>UpdateLogRequest</em> XML document. Contents of the specified paths are retained, while all others are removed. All content is retained if no filters are specified; filters are only necessary if the intent is to not retain some part or parts of the content.</td></tr><tr><td>Filters/Filter/@type</td><td>F</td><td>Identifier of the UpdateLogRequest/LogContent child element affected by the correseponding <strong>value</strong>. It may be either blank, <em>ESPContext</em>, <em>UserContext</em>, <em>UserRequest</em>, <em>UserResponse</em>, <em>BackEndResponse</em>. If blank, the corresponding <strong>value</strong> applies to the complete document. If <em>BackEndResponse</em>, <strong>value</strong> is ignored and the entire section is retained.</td></tr><tr><td>Filters/Filter/@value</td><td>F</td><td>XPath relative to the indicated document section, or complete document, of an element to be retained in the log update request. If blank, the entire section is retained.</td></tr><tr><td>@MaxLogQueueLength / <s>MaxLogQueueLength</s></td><td>T</td><td>Maximum log update request queue length. It is an error to attempt to add more than this number of requests to the request queue. Omitted or empty defaults to <em>500,000</em>.</td></tr><tr><td>@MaxRetriesRS / <s>MaxRetriesRS</s></td><td>T</td><td>Maximum number of attempts to resent a log update request if sending fails. Omitted or empty defaults to <em>-1</em>, which has the effect of not limiting the attempts.</td></tr><tr><td>@PendingLogBufferSize / <s>PendingLogBufferSize</s></td><td>T</td><td>Maximum number of pending log update requests that can be queued before moving them into the main request queue. Omitted or empty defaults to <em>100</em>.</td></tr><tr><td>@QueueSizeSignal / <s>QueueSizeSignal</s></td><td>T</td><td>Defines a log update request queue size threshold for which a trace log error message is generated when the queue size reaches any multiple of the threshold. If set to <em>5,000</em>, then an error message will be recorded when the queue size reaches <em>5,000</em>, <em>10,000</em>, <em>15,000</em>, etc. Omitted or empty defaults to <em>10,000</em>.</td></tr><tr><td>@ReadRequestWaitingSeconds /<s>ReadRequestWaitingSeconds</s></td><td>T</td><td>Polling interval, in seconds, for FailSafe log file update checks. Omitted or empty defaults to <em>15</em>.</td></tr><tr><td>@SafeRolloverThreshold / <s>SafeRolloverThreshold</s></td><td>S</td><td>Maximum size of a FailSafe log file before it is closed and a new, empty file is created in its place. The format of the value is a scalar followed by an optional unit. If the unit is omitted, the scalar represents a number of bytes. With units of <em>K</em>, <em>M</em>, <em>G</em>, or <em>T</em>, the scalar represents a number of kilobytes, megabytes, gigabytes, or terabytes. In reality, the total number of bytes should not exceed four gigabytes due to internal limits. Omitted or empty defaults to <em>0</em>, implying no size constraint.</td></tr><tr><td>Variant</td><td>A</td><td>Repeating element containing user-defined identifiers for an agent. ESDL services which configure agent instances that do not apply to every transaction may reference these identifiers in custom transforms to manage which agents are applied to a transaction.</td></tr><tr><td>Variant/@type</td><td>A</td><td>Identifier of a single kind of log content generated by the agent. An agent that will not be controlled by content kind may omit this identifier. An agent that will be controlled by content kind should specify a unique identifier for each kind of content to be controlled. In custom transform parlance, this concept maps to a log option.</td></tr><tr><td>Variant/@group</td><td>A</td><td>Identifier for a collection of agents that are used together. Given a service with agents A, B, C, and D, some service methods may only use A and B, while others may only use C and D. A and B can be grouped by name, as can C and D, such that a new agent E may be seamlessly added to the group of A and B. In custom transform parlance, this concept maps to a log profile.</td></tr></tbody></table><p>In the case of a split configuration, these properties must exist in the base configuration. Repeating them in the external property file will cause no harm, but externally defined values will have no effect on the logging manager or other logging components.</p><h4 id="modular-properties" tabindex="-1"><em>Modular</em> Properties <a class="header-anchor" href="#modular-properties" aria-label="Permalink to &quot;*Modular* Properties&quot;">​</a></h4><table tabindex="0"><thead><tr><th>XPath</th><th>Description</th></tr></thead><tbody><tr><td>@configuration</td><td>Optional path to an external configuration file. If omitted or empty, the configuration is inline. If not empty, the configuration is split.</td></tr><tr><td>@module</td><td>Optional plugin-defined identifier for which <strong>LogAgent</strong> implementation to use. Omitted or empty assume <em>default</em>. In a split configuration, only the value in the external property file is honored.</td></tr><tr><td>@name</td><td>Optional user-defined identifier used in trace log output. In an inline configuration, the value is required as an <em>agent integration</em> property. In a split configuration and in the external property file, the value is not required to match the value of the corresponding <em>agent integration</em> property.</td></tr><tr><td>@trace-priority-level</td><td>Optional value of the maximum trace log output message severity to be included in trace log output, given on a scale of <em>-1</em> to <em>100</em>. Omitted or empty assumes <em>-1</em>, which inherits a limit derived from the ESP log level. Honored in an inline configuration; ignored in a split configuration.</td></tr></tbody></table><h4 id="default-implementation" tabindex="-1">&quot;default&quot; Implementation <a class="header-anchor" href="#default-implementation" aria-label="Permalink to &quot;&quot;default&quot; Implementation&quot;">​</a></h4><table tabindex="0"><thead><tr><th>XPath</th><th>Description</th></tr></thead><tbody><tr><td>@UpdateLog</td><td>Optional Boolean flag instructing the agent to use either the <em>default</em> <strong>UpdateLog</strong> or no <strong>UpdateLog</strong>. It must not be combined with an <strong>UpdateLog</strong> child property. In the absence of <strong>UpdateLog</strong>, an omitted, empty, or <em>false</em> value prevent log update requests from being processed.</td></tr><tr><td>UpdateLog</td><td>Optional configuration of a module intended to process update log requests. It must not be combined with an <strong>@UpdateLog</strong> property. In the absence of <strong>@UpdateLog</strong>, omission prevents log update requests from being processed.</td></tr></tbody></table><p>The default <strong>UpdateLog</strong>, described in the next section, has little to no practical value in a live deployment. In fact, writing potential PII to trace log output should be seen as a security risk. It should not be enabled by default.</p><p>The default <strong>UpdateLog</strong> does add value in development and testing environments. It enables verification of the logging framework, confirming that the expected data reaches the intended agents. It also provides verification that ESDL transformations performed by the transactional ESP produce the expected content.</p><p>If one assumes that an empty <strong>UpdateLog</strong> element implies the default behavior, because it contains no directions to deviate from its default behavior, one could also assume that including an empty element is sufficient to request the default behavior. This assumption is valid with XML markup. YAML markup doesn&#39;t handle empty elements, at least not well.</p><p>For YAML compatibility, an <strong>@UpdateLog</strong> scalar value is defined. A YAML property file may specify <em>UpdateLog: true</em> to enable the default behavior. A property file may specify either <strong>@UpdateLog</strong> or <strong>UpdateLog</strong>, but not both. Including both is an error and the log update service will not be available.</p><h4 id="mock-implementation" tabindex="-1">&quot;mock&quot; Implementation <a class="header-anchor" href="#mock-implementation" aria-label="Permalink to &quot;&quot;mock&quot; Implementation&quot;">​</a></h4><table tabindex="0"><thead><tr><th>XPath</th><th>Description</th></tr></thead><tbody><tr><td>GetTransactionSeed</td><td>Optional element to enable the <em>GetTransactionSeed</em> service. The return value of <code>getTransactionSeed</code> is derived from the value of <code>@status-code</code>.</td></tr><tr><td>GetTransactionSeed/@seed-id</td><td>Optional value to populate IEspGetTransactionSeedResponse::SeedId.</td></tr><tr><td>GetTransactionSeed/@status-code</td><td>Optional value to populate IEspGetTransactionSeedResponse::StatusCode.</td></tr><tr><td>GetTransactionSeed/@status-message</td><td>Optional value to populate IEspGetTransactionSeedResponse::StatusMessage</td></tr><tr><td>GetTransactionId</td><td>Optional element to enable the <em>GetTransactionID</em> service.</td></tr><tr><td>GetTransactionId/@id</td><td>Optional value to populate the <code>transactionID</code> parameter.</td></tr><tr><td>UpdateLog</td><td>Optioonal element to enable the <em>UpdateLOG</em> service.</td></tr><tr><td>UpdateLog/@response</td><td>Optional value to populate IEspUpdateLogResponse::Response.</td></tr><tr><td>UpdateLog/@status-code</td><td>Optional value to populate IEspUpdateLogResponse::StatusCode.</td></tr><tr><td>UpdateLog/@status-message</td><td>Optional value to populate IEspUpdateLogResponse::StatusMessage.</td></tr></tbody></table><h3 id="logagent-updatelog" tabindex="-1">//LogAgent/UpdateLog <a class="header-anchor" href="#logagent-updatelog" aria-label="Permalink to &quot;//LogAgent/UpdateLog&quot;">​</a></h3><h4 id="modular-properties-1" tabindex="-1"><em>Modular</em> Properties <a class="header-anchor" href="#modular-properties-1" aria-label="Permalink to &quot;*Modular* Properties&quot;">​</a></h4><table tabindex="0"><thead><tr><th>XPath</th><th>Description</th></tr></thead><tbody><tr><td>@disabled</td><td>Optional Boolean flag allowing a configured module to be ignored (<em>true</em>) or honored (<em>false</em>). Omitted or empty defaults to <em>false</em>.</td></tr><tr><td>@module</td><td>Optional plugin-defined identifier for which <strong>UpdateLog</strong> implementation module to use. Omitted or empty assumes &quot;default&quot;.</td></tr><tr><td>@name</td><td>Optional module identifier used in trace log output. No default is applied.</td></tr><tr><td>@trace-priority-level</td><td>Optional value of the maximum trace log output message severity to be included in trace log output, given on a scale of <em>-1</em> to <em>100</em>. Omitted or empty assumes <em>-1</em>, which inherits from <strong>LogAgent</strong>.</td></tr></tbody></table><h4 id="default-implementation-1" tabindex="-1">&quot;default&quot; Implementation <a class="header-anchor" href="#default-implementation-1" aria-label="Permalink to &quot;&quot;default&quot; Implementation&quot;">​</a></h4><table tabindex="0"><thead><tr><th>XPath</th><th>Description</th></tr></thead><tbody><tr><td>Target</td><td>Optional configuration of a content destination used to complete a log update. Absence of the element causes the text buffer that would have been sent to a configured <strong>Target</strong> module to be recorded in trace log output, with priority <em>90</em>. Presence of the element causes the text buffer produced by this module to be passed to the configured <strong>Target</strong> module.</td></tr></tbody></table><h3 id="updatelog-target" tabindex="-1">//UpdateLog/Target <a class="header-anchor" href="#updatelog-target" aria-label="Permalink to &quot;//UpdateLog/Target&quot;">​</a></h3><h4 id="modular-properties-2" tabindex="-1"><em>Modular</em> Properties <a class="header-anchor" href="#modular-properties-2" aria-label="Permalink to &quot;*Modular* Properties&quot;">​</a></h4><table tabindex="0"><thead><tr><th>XPath</th><th>Description</th></tr></thead><tbody><tr><td>@disabled</td><td>Optional Boolean flag allowing a configured module to be ignored (<em>true</em>) or honored (<em>false</em>). Omitted or empty defaults to <em>false</em>.</td></tr><tr><td>@module</td><td>Optional plugin-defined identifier for which <strong>Target</strong> implementation module to use. Omitted or empty assumes &quot;default&quot;.</td></tr><tr><td>@name</td><td>Optional module identifier used in trace log output. No default is applied.</td></tr><tr><td>@trace-priority-level</td><td>Optional value of the maximum trace log output message severity to be included in trace log output, given on a scale of <em>-1</em> to <em>100</em>. Omitted or empty assumes <em>-1</em>, which inherits from <strong>UpdateLog</strong>.</td></tr></tbody></table><h4 id="default-implementation-2" tabindex="-1">&quot;default&quot; Implementation <a class="header-anchor" href="#default-implementation-2" aria-label="Permalink to &quot;&quot;default&quot; Implementation&quot;">​</a></h4><p>N/A</p><h4 id="file-implementation" tabindex="-1">&quot;file&quot; Implementation <a class="header-anchor" href="#file-implementation" aria-label="Permalink to &quot;&quot;file&quot; Implementation&quot;">​</a></h4><table tabindex="0"><thead><tr><th>XPath</th><th>Description</th></tr></thead><tbody><tr><td>@filepath</td><td>Required filesystem path specifying the location of the log file. While the given value may be a valid path, it may be given as a pattern that includes one or more variable substitutions. Variable substitutions are described in depth immediately following this table.</td></tr><tr><td>@module</td><td>Required module key label used to select file logging. The value must be <em>file</em>.</td></tr><tr><td>@concurrent-files</td><td>Optional upper bound for the number of open files managed by instance at any given time. Variable substitions in <strong>@filepath</strong> make multiple paths possible, and this value prevents unbounded resource consumption. If omitted or empty, the value is assumed to be <em>1</em>. The value must be between 1 and 255.</td></tr><tr><td>@debug</td><td>Optional Boolean flag to control whether only the <em>final</em> input (<em>false</em>) or all (<em>true</em>) input is written to the file. If omitted or empty, the value is assumed to be <em>false</em>.</td></tr><tr><td>@format-creation-date</td><td>Optional override of the default date format string used when <em>creation:date</em> is included in <strong>@filepath</strong>. If omitted or empty, the value is assumed to be &quot;%Y_%m_%d&quot;. When given, the value must include &quot;%Y&quot;, &quot;%m&quot;, and &quot;%d&quot; for strftime formatting.</td></tr><tr><td>@format-creation-time</td><td>Optional override of the default time format string used when <em>creation:time</em> is included in <strong>@filepath</strong>. If omitted or empty, the value is assumed to be &quot;%H_%M_%S&quot;. When given, the value must include &quot;%H&quot;, &quot;%M&quot;, and &quot;%S&quot; for strftime formatting.</td></tr><tr><td>@format-creation-datetime</td><td>Optional override of the default date and time format string used when either <em>creation</em> or <em>creation:datetime</em> is included in <strong>@filepath</strong>. If omitted or empty, the value is assumed to be &quot;%Y_%m_%d_%H_%M_%S&quot;. When given, the value must include &quot;%Y&quot;, &quot;%m&quot;, &quot;%d&quot;, &quot;%H&quot;, &quot;%M&quot;, and &quot;%S&quot;.</td></tr><tr><td>@rollover-interval</td><td>Optional specification of the frequency at which a new file is automatically created, regardless of the size of the current file. Must be either &quot;none&quot;, indicating no timed rollover, or &quot;daily&quot;, indicating a rollover on a date change. If omitted or empty, the value is assumed to be &quot;daily&quot;. A rollover file will only be created to record a transaction&#39;s data; an instance with no activity for a complete calendar day should create no file for that calendar day.</td></tr><tr><td>@rollover-size</td><td>Optional specification of the maximum size for a file before a new file is automatically created. If omitted or empty, the value is assumed to be &quot;0&quot;. A value of zero disables size-based rollover; a positive number by itself is bytes; a positive number followed by &#39;k&#39; or &#39;K&#39; is kilobytes; a positive number followed by &#39;m&#39; or &#39;M&#39; is megabytes; a positive number followed by &#39;g&#39; or &#39;G&#39; is gigabytes; a positive number followed by &#39;t&#39; or &#39;T&#39; is terabytes; any other value is invalid. Each file is guaranteed to contain at least one complete transaction regardless of the configured constraint.</td></tr><tr><td>@trace-priority-limit</td><td>Optional upper bound on which trace output entries will be recorded, given on a scale of <em>-1</em> to <em>100</em>. If omitted or empty, the value is assumed to be <em>-1</em>. A value of <em>-1</em> instructs the module to inherit its constraint from the containing <strong>UpdateLog</strong> instance.</td></tr></tbody></table><p>Variable substitutions enable an instance to dynamically adjust the location and name of an output file based on information obtained from the UpdateLogRequest XML document. This flexibility may not be crucial for dynamic binding configurations when most of the values are known, but it is needed for configurations that cannot easily be tailored to a binding. Variables are represented in <strong>@filepath</strong> using the &quot;{$&quot; <code>&lt;name&gt;</code> [ &quot;:&quot; <code>&lt;option&gt;</code> ] &quot;}&quot;&#39; markup. All variable references must specify a name, while most will not include an option. The accepted variables are:</p><table tabindex="0"><thead><tr><th>Name</th><th>Option</th><th>Description</th></tr></thead><tbody><tr><td>binding</td><td>N/A</td><td>Substitutes the dynamic ESDL binding ID, which is comprised of the process name, port number, and ESDL service name.</td></tr><tr><td>creation</td><td></td><td>Substitutes the current date and time at the moment of file creation. An empty option is equivalent to the <em>datetime</em> option.</td></tr><tr><td>creation</td><td>custom</td><td>For the first file created by an instance, substitutes the current date at the moment of file creation. If a file exists with the substituted value, it substitutes the current date and time at the moment of file creation.</td></tr><tr><td>creation</td><td>date</td><td>Substitutes the current date at the moment of file creation.</td></tr><tr><td>creation</td><td>datetime</td><td>Substitutes the current date and time at the moment of file creation.</td></tr><tr><td>creation</td><td>time</td><td>Substitutes the current time at the moment of file creation.</td></tr><tr><td>creation</td><td><code>&lt;format&gt;</code></td><td>Substitutes the current date or time at the moment of file creation, according to the strftime format string specified.</td></tr><tr><td>esdl-method</td><td>N/A</td><td>Substitutes the ESDL-defined method name.</td></tr><tr><td>esdl-service</td><td>N/A</td><td>Substitutes the ESDL-defined service name.</td></tr><tr><td>port</td><td>N/A</td><td>Substitutes the port number on which the transaction occurred.</td></tr><tr><td>process</td><td>N/A</td><td>Substitutes the configured process name from which the transaction occurred.</td></tr><tr><td>service</td><td>N/A</td><td>Substitutes the configured service name from which the transaction occurred.</td></tr></tbody></table><p>Given a transactional process named &quot;myesdlesp&quot; and a service named &quot;myservice&quot;, a <strong>@filepath</strong> value of <em>/var/log/HPCCSystems/{$process}/{$service}/log.{$creation:custom}.csv</em> could resolve to <em>/var/log/HPCCSystems/myesdlesp/myservice/log.2020_12_31.csv</em> for a transaction occurring on December 31, 2020.</p>`,131)]))}const h=t(i,[["render",r]]);export{p as __pageData,h as default};
