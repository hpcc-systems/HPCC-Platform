import{_ as t,a,o as n,ag as s}from"./chunks/framework.Do1Zayaf.js";const u=JSON.parse('{"title":"Data Masking Framework","description":"","frontmatter":{},"headers":[],"relativePath":"system/masking/include/readme.md","filePath":"system/masking/include/readme.md","lastUpdated":1755191786000}'),o={name:"system/masking/include/readme.md"};function i(l,e,r,p,c,d){return n(),a("div",null,e[0]||(e[0]=[s(`<h1 id="data-masking-framework" tabindex="-1">Data Masking Framework <a class="header-anchor" href="#data-masking-framework" aria-label="Permalink to &quot;Data Masking Framework&quot;">​</a></h1><p>This is a high level description of the data obfuscation framework defined in <code>system/masking/include</code>. The framework includes a platform component, the engine, that exposes obfuscation logic supplied by dynamically loaded plugin libraries. The framework can be used to obfuscate sensitive data, such as passwords and PII. Possible use cases including preventing trace output from revealing sensitive values and partially masking values in situations where a user needs to see &quot;enough&quot; of a value to confirm its correctness without seeing all of the value (e.g., when a user is asked to confirm the last four digits of an account number).</p><table tabindex="0"><thead><tr><th>File</th><th>Contents</th></tr></thead><tbody><tr><td>datamasking.h</td><td>Declares the core framework interfaces. Most are used by the engine and plugins.</td></tr><tr><td>datamaskingengine.hpp</td><td>Defines the engine component from the core engine interface.</td></tr><tr><td>datamaskingplugin.hpp</td><td>Defines a set of classes derived from the core plugin interfaces that can be used to provide rudimentary obfuscation. It is expected that the some or all classes will be subclassed, if not replaced outright, in new plugins.</td></tr><tr><td>datamaskingshared.hpp</td><td>Defines utilities shared by engine and plugin implementations.</td></tr></tbody></table><h2 id="glossary" tabindex="-1">Glossary <a class="header-anchor" href="#glossary" aria-label="Permalink to &quot;Glossary&quot;">​</a></h2><h3 id="domain" tabindex="-1">Domain <a class="header-anchor" href="#domain" aria-label="Permalink to &quot;Domain&quot;">​</a></h3><p>A <em>domain</em> is a representation of the obfuscation requirements applicable to a set of data.</p><p>Consider that the data used to represent individuals likely differs between countries. With different data, requirements for obfuscation may reasonably be expected to vary. Assuming that requirements do change between countries, each country&#39;s requirements could logically constitute a separate domain. The capacity to define multiple <em>domains</em> does not create a requirement to do so.</p><p>Requirements can change over time. To support this, a <em>domain</em> can be seen as a collection of requirement snapshots where each snapshot defines the complete set of requirements for the <em>domain</em> at a point in time. Snapshots are referenced by unique version numbers, which should be sequential starting at <em>1</em>.</p><p>Obfuscation is always applied based on a single snapshot of a domain&#39;s requirements.</p><p><em>Domains</em> are represented in the framework interface as text identifiers. Each distinct <em>domain</em> is identified by at least one unique identifier.</p><h3 id="masker" tabindex="-1">Masker <a class="header-anchor" href="#masker" aria-label="Permalink to &quot;Masker&quot;">​</a></h3><p>A <em>masker</em> is a provider of obfuscation for a single snapshot of a <em>domain&#39;s</em> requirements. There are three masking operations defined in this framework. Each instance decides which of the three it will support, and how it will support them. The three operations are:</p><ul><li><code>maskValue</code> obfuscates individual values based on snapshot-defined meanings. For example, a value identified as a password might require complete obfuscation anywhere it appears, or an account number may require complete obfuscation in some cases and partial obfuscation in others.</li><li><code>maskContent</code> obfuscates a variable number of values based on context provided by surrounding text. For example, the value of an HTTP <em>authentication</em> header or the text between <code>&lt;Password&gt;</code> and <code>&lt;/Password&gt;</code> might require obfuscation. This operation can apply to both structured and unstructured text content.</li><li><code>maskMarkupValue</code> obfuscates individual values based on their locationa within an in-memory representation of a structured document, such as XML or JSON. For example, the value of element <code>Password</code> might require obfuscation unconditionally, while the value of element <code>Value</code> might require obfuscation only if a sibling element named <code>Name</code> exists with value <em>password</em>. This operation relies on the caller&#39;s ability to supply context parsed from structured content.</li></ul><p>A masker may be either stateless or stateful. With a stateless masker, identical input will produce identical output for every requested operation. A stateful masker, however, enables its user to affect operation outputs (i.e., identical input may not produce identical output for each operation).</p><p><em>Maskers</em> are represented in the framework interface using <code>IDataMasker</code>, with <code>IDataMaskerInspector</code> providing access to less frequently used information about the <em>domain</em>.</p><h3 id="profile" tabindex="-1">Profile <a class="header-anchor" href="#profile" aria-label="Permalink to &quot;Profile&quot;">​</a></h3><p>A <em>profile</em> is a stateless <em>masker</em>. Each instance defines the requirements of one or more snapshots of a single <em>domain</em>.</p><p>Snapshots are versioned. Each <em>profile</em> declares a minimum, maximum, and default version. <em>Masker</em> operations apply to the default version. Other declared versions may be accessed using a stateful <em>context</em>, which the <em>profile</em> can create on demand.</p><p>Each instance must support at least one version of a <em>domain&#39;s</em> requirements. Whether an instance supports more than one version depends on the implementation and on user preference. A <em>domain</em> can be viewed as a collection of one or more <em>profiles</em> where each <em>profile</em> defines a unique set of requirement snapshots applicable to the same underlying data.</p><p>Refer to <code>IDataMaskingProfile</code> (extending <code>IDataMasker</code>) and <code>IDataMaskingProfileInspector</code> (extending <code>IDataMaskerInspector</code>) for additional information.</p><h3 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h3><p>A <em>context</em> is a stateful <em>masker</em>. Instantiated by and tightly coupled to a <em>profile</em>, it provides some user control over how masking operations are completed.</p><ul><li>For a <em>profile</em> supporting multiple versions, the requirements of a non-default version may be applied.</li><li>Custom properties, defined by a <em>profile</em>, may be managed. <ul><li><code>valuetype-set</code> is a pre-defined property used to select the group of <em>value types</em> that may be masked by any operation.</li><li><code>rule-set</code> is a pre-defined property used to select the group of <em>rules</em> that will be applied for <code>maskContent</code> requests.</li><li><em>Profile</em> implementations may define additional properties as needed. For example, one might define <code>mask-pattern</code> to override the default obfuscation pattern to avoid replicating (and complicating) configurations just to change the appearance of obfuscated data.</li></ul></li><li>Trace output produced by operation requests, including errors and warnings encountered during request processing, can be controlled per context. This does not affect the operation output, per-se, but provides compatibility with transactional trace output control.</li></ul><p>Refer to <code>IDataMaskingProfileContext</code> (extending <code>IDataMasker</code>) and <code>IDataMaskingProfileContextInspector</code> (extending <code>IDataMaskerInspector</code>) for additional information.</p><h3 id="value-type" tabindex="-1">Value Type <a class="header-anchor" href="#value-type" aria-label="Permalink to &quot;Value Type&quot;">​</a></h3><p>Each snapshot defines one or more <em>value types</em>. A <em>value type</em> is a representation of the requirements pertaining to a particular concept of a <em>domain</em> datum. Requirements include:</p><ul><li>instructions for identifying occurrences based on contextual clues found in a body of text; and</li><li>instructions for applying obfuscation to an identified value.</li></ul><p>A Social Security Number, or SSN, is a U.S.-centric datum that requires obfuscation and for which a <em>value type</em> may be defined. Element names associated with an SSN may include, but are not limited to, <em>SSN</em> and <em>SOCS</em>; the <em>value type</em> is expected to identify all such names used within the <em>domain</em>. SSN occurrences are frequently partially masked, with common formats being to mask only the first four or the last five digits of the nine digit number; the <em>value type</em> defines which formats are available besides the default of masking all characters.</p><p><em>Value types</em> are represented in the framework using <code>IDataMaskingProfileValueType</code>. They are accessed through the <code>IDataMaskerInspector</code> interface.</p><h3 id="mask-style" tabindex="-1">Mask Style <a class="header-anchor" href="#mask-style" aria-label="Permalink to &quot;Mask Style&quot;">​</a></h3><p>A <em>mask style</em> describes how obfuscation is applied to a value. It is always defined in the context of a <em>value type</em>, and a <em>value type</em> may define multiple.</p><p>A <em>value type</em> is not required to define any <em>mask styles</em>. If none are defined, all value characters are obfuscated. If the requested <em>mask style</em> is not defined, the default obfuscation occurs; the <em>value type</em> will not attempt to guess which of the defined styles is appropriate.</p><p><em>Mask styles</em> are represented in the framework using <code>IDataMaskingProfileMaskStyle</code>. They are accessed through the <code>IDataMaskingProfileValueType</code> interface.</p><h3 id="rule" tabindex="-1">Rule <a class="header-anchor" href="#rule" aria-label="Permalink to &quot;Rule&quot;">​</a></h3><p>A <em>rule</em> contains the information necessary to locate at least one occurrence of a <em>value type</em> datum to be obfuscated. It is always defined in the context of a <em>value type</em>, and a <em>value type</em> may define multiple.</p><p><em>maskValue</em> requests do not use <em>rules</em>. <em>maskContent</em> requests rely on <em>rules</em> for locating affected values, with the relationship between a <em>profile</em> and its <em>rules</em> an implementation detail. <em>maskMarkupValue</em> requests, when implemented, may also use <em>rules</em> or may take an entirely different approach.</p><p><em>Rules</em> are represented in the framework as an abastract concept that cannot be inspected individually. Inspection may be used to establish the presence of <em>rules</em>, but not to examine individual instances.</p><h3 id="plugin" tabindex="-1">Plugin <a class="header-anchor" href="#plugin" aria-label="Permalink to &quot;Plugin&quot;">​</a></h3><p>The combination of a shared library and entry point function describes a <em>plugin</em>. The input to a <em>plugin</em> is a property tree describing one or more <em>profiles</em>. The output of an entry point function is an iterator of <em>profiles</em>. The <em>profiles</em> created by the function may all be associated with the same <em>domain</em>, but this is not required.</p><p><em>Plugin</em> results are represented in the framework using <code>IDataMaskingProfileIterator</code>.</p><h3 id="engine" tabindex="-1">Engine <a class="header-anchor" href="#engine" aria-label="Permalink to &quot;Engine&quot;">​</a></h3><p>An <em>engine</em> is the platform&#39;s interface to obfuscation. It loads <em>domains</em> by loading one or more <em>plugins</em>. <em>Plugins</em> yield <em>profiles</em>, from which <em>domains</em> are inferred.</p><p>Once configured with at least one <em>domain</em>, a caller can obtain obfuscation in multiple ways:</p><ol><li>As an instance of a stateless <em>masker</em>, an <em>engine</em> can provide obfuscation. The default version of the default <em>profile</em> of the default <em>domain</em> will be used, and no custom <em>context</em> properties can be used. This is the simplest integration, appropriate for use when the host process implicitly trusts that the default configuration is sufficient.</li><li>An <em>engine</em> may be used to obtain a stateless <em>profile</em> for a specific <em>domain</em>. If no version is requested, the default <em>profile</em> of the requested <em>domain</em> will be used. If a version is specified, the <em>domain</em> <em>profile</em> supporting the requested version is used. This usage supports host processes that are aware of <em>domains</em> and need to use specific instances.</li><li>An <em>engine</em> may be used to obtain a <em>context</em> tied to a specific version of a <em>domain</em>. The default <em>domain</em> may be requested with an empty string. The default version may be requested by passing <em>0</em>. With a <em>context</em>, a caller may manipulate further the environment for obfuscation requests.</li></ol><p><em>Engines</em> are represented in the framework using <code>IDataMaskingEngine</code> (extending <code>IDataMasker</code>) and <code>IDataMaskingEngineInspector</code> for additional information.</p><h2 id="environment" tabindex="-1">Environment <a class="header-anchor" href="#environment" aria-label="Permalink to &quot;Environment&quot;">​</a></h2><p>This section assumes a <em>context</em> is in use. Absent a <em>context</em>, only the default state of the default version of a <em>profile</em> can be used.</p><p>The framework reserves multiple custom property names, which are described in subsequent subsections. It also allows implementations to define additional properties using any non-reserved name.</p><p>Suppose an implementation defines a property to override the default obfuscation pattern. Let&#39;s call this property <code>default-pattern</code>. A caller could interrogate a <em>masker</em> to know if <code>default-pattern</code> is accepted. If accepted, <code>setProperty(&quot;default-patterns&quot;, &quot;#&quot;)</code> can be used to register an override.</p><p>All custom properties, whether defined in the framework or in third party libraries, are managed using a generic <em>context</em> interface. This interface includes:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>bool hasProperties() cosnt</span></span>
<span class="line"><span>bool hasProperty(const char* name) const</span></span>
<span class="line"><span>const char* queryProperty(const char* name) const</span></span>
<span class="line"><span>bool setProperty(const char* name, const char* value)</span></span>
<span class="line"><span>bool removeProperty(const char* name)</span></span></code></pre></div><h3 id="value-type-sets" tabindex="-1">Value Type Sets <a class="header-anchor" href="#value-type-sets" aria-label="Permalink to &quot;Value Type Sets&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>bool setProperty(const char* name, const char* value);</span></span></code></pre></div><h4 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h4><p>The abstraction includes the concept of sets of related <em>value types</em>. All <em>value types</em> should be assigned membership in at least one set. One set should be selected by default. The custom <em>context</em> property <strong>valuetype-set</strong> is used to select a different set.</p><p>The rationale for this is an expectation that certain data always requires obfuscation. A password, for example, would never not require obfuscation. Other data may only require obfuscacation in certain situations, such as when required by individual customer agreements. Callers should not be required to complete additional steps to act on data that must always be obfuscated, and should not need to know which data falls in which category.</p><blockquote><p>The set name &quot;*&quot; is reserved to select all <em>value types</em> regardless of their defined set membership. This mechanism is intended to assist with compatibility checks, and should be used with care in other situations.</p></blockquote><h4 id="runtime-compatibility" tabindex="-1">Runtime Compatibility <a class="header-anchor" href="#runtime-compatibility" aria-label="Permalink to &quot;Runtime Compatibility&quot;">​</a></h4><p>Use <code>acceptsProperty</code> to determine whether a snapshot recognizes a custom property name. Use <code>usesProperty</code> to learn if the snapshot includes references to the custom property name. Acceptance implies awareness of the set (and what it represents) even if selecting it will not change the outcome of any masking request. Usage implies that selecting the set will change the outcome of at least one masking operation.</p><p>The property <code>valuetype-set</code> is used to select a named set. A check of this property addresses whether the concept of a set is accepted or used by the snapshot.</p><blockquote><p>For improved compatibility checks, implementations are encouraged to reserve additional property names that enable checks for individual set names. For each set name, <em>foo</em>, the included implementations report property <code>valuetype-set:foo</code> as used.</p></blockquote><h4 id="implementation" tabindex="-1">Implementation <a class="header-anchor" href="#implementation" aria-label="Permalink to &quot;Implementation&quot;">​</a></h4><p>The included implementations allow membership in either a default, unnamed set or in any number of named sets. Absent a contextual request for a named set, the unnamed set is selected by default. With a contextual set request, the members of the named set are selected in addition to members of the unnamed set. Members of the unnamed set are never not selected.</p><blockquote><p>Unnamed set membership cannot be defined explicitly. Because this set is always selected, assigning a <em>value type</em> to both a named and the unnamed set is redundant - the type will be selected whether the named set is requested or not.</p></blockquote><p><em>Example 1a: default value type</em></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>profile:</span></span>
<span class="line"><span>  valueType:</span></span>
<span class="line"><span>    - name: type1</span></span></code></pre></div><p><em>Value type</em> <code>type1</code> belongs to the default, unnamed <em>value type</em> set. Property <code>valuetype-set</code> is accepted, but not used; an attempt to use it will change no results.</p><p><em>Example 1b: sample set memberships</em></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>profile:</span></span>
<span class="line"><span>  valueType:</span></span>
<span class="line"><span>    - name: type1</span></span>
<span class="line"><span>    - name: type2</span></span>
<span class="line"><span>      memberOf:</span></span>
<span class="line"><span>        - name: set1</span></span>
<span class="line"><span>        - name: set2</span></span>
<span class="line"><span>    - name: type3</span></span>
<span class="line"><span>      memberOf:</span></span>
<span class="line"><span>        - name: set2</span></span>
<span class="line"><span>        - name: set3</span></span>
<span class="line"><span>    - name: type4</span></span>
<span class="line"><span>      memberOf:</span></span>
<span class="line"><span>        - name: set1</span></span>
<span class="line"><span>    - name: type5</span></span>
<span class="line"><span>      memberOf:</span></span>
<span class="line"><span>        - name: set3</span></span></code></pre></div><p>Extending the previous example, five types and four sets are in use. Property <code>valuetype-set</code> is both accepted and used, as it can now impact results. Properties <code>valuetype-set:set1</code>, <code>valuetype-set:set2</code> and <code>valuetype-set:set3</code> are also both accepted and used, but are intended only for use with compatibility checks.</p><p>This table shows which types are selected for each requested set:</p><table tabindex="0"><thead><tr><th style="text-align:right;"></th><th style="text-align:center;"><em>unnamed</em></th><th style="text-align:center;">set1</th><th style="text-align:center;">set2</th><th style="text-align:center;">set3</th><th style="text-align:center;">*</th></tr></thead><tbody><tr><td style="text-align:right;">type1</td><td style="text-align:center;">Y</td><td style="text-align:center;">Y</td><td style="text-align:center;">Y</td><td style="text-align:center;">Y</td><td style="text-align:center;">Y</td></tr><tr><td style="text-align:right;">type2</td><td style="text-align:center;">N</td><td style="text-align:center;">Y</td><td style="text-align:center;">Y</td><td style="text-align:center;">N</td><td style="text-align:center;">Y</td></tr><tr><td style="text-align:right;">type3</td><td style="text-align:center;">N</td><td style="text-align:center;">N</td><td style="text-align:center;">Y</td><td style="text-align:center;">Y</td><td style="text-align:center;">Y</td></tr><tr><td style="text-align:right;">type4</td><td style="text-align:center;">N</td><td style="text-align:center;">Y</td><td style="text-align:center;">N</td><td style="text-align:center;">N</td><td style="text-align:center;">Y</td></tr><tr><td style="text-align:right;">type5</td><td style="text-align:center;">N</td><td style="text-align:center;">N</td><td style="text-align:center;">N</td><td style="text-align:center;">Y</td><td style="text-align:center;">Y</td></tr></tbody></table><p><em>Example 1c: sample accepted sets</em></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>profile:</span></span>
<span class="line"><span>  valueType:</span></span>
<span class="line"><span>    - name: type1</span></span>
<span class="line"><span>    - name: type2</span></span>
<span class="line"><span>      memberOf:</span></span>
<span class="line"><span>        - name: set1</span></span>
<span class="line"><span>        - name: set2</span></span>
<span class="line"><span>    - name: type3</span></span>
<span class="line"><span>      memberOf:</span></span>
<span class="line"><span>        - name: set2</span></span>
<span class="line"><span>        - name: set3</span></span>
<span class="line"><span>    - name: type4</span></span>
<span class="line"><span>      memberOf:</span></span>
<span class="line"><span>        - name: set1</span></span>
<span class="line"><span>    - name: type5</span></span>
<span class="line"><span>      memberOf:</span></span>
<span class="line"><span>        - name: set3</span></span>
<span class="line"><span>  property:</span></span>
<span class="line"><span>    - name: &#39;valuetype-set:set4&#39;</span></span>
<span class="line"><span>    - name: &#39;valuetype-set:set5&#39;</span></span></code></pre></div><p>Continuing the previous example, the <em>profile</em> has declared acceptance of two additional sets using properties <code>valuetype-set:set4</code> and <code>valuetype-set:set5</code>. Selection of these sets will not change results, but a caller might accept acceptance of a set as sufficient to establish compatibility.</p><h3 id="rule-sets" tabindex="-1">Rule Sets <a class="header-anchor" href="#rule-sets" aria-label="Permalink to &quot;Rule Sets&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>bool setProperty(const char* name, const char* value);</span></span></code></pre></div><h4 id="overview-1" tabindex="-1">Overview <a class="header-anchor" href="#overview-1" aria-label="Permalink to &quot;Overview&quot;">​</a></h4><p>The abstraction includes the concept of sets of related <em>rules</em>. All <em>rules</em> should be assigned membership in at least one set. One set should be selected by default. The custom <em>context</em> property <strong>rule-set</strong> is used to select a different set.</p><p>The rationale for this is similar to yet different than that of <em>value type</em> sets. Instead of one set of <em>rules</em> that are always selected, backward compatibility with a proprietary legacy implementation requires that the default set be replaced by an alternate collection.</p><blockquote><p>The set name &quot;*&quot; is reserved to select all <em>rules</em> regardless of their defined set membership. This does not override constraints imposed by the current <em>value type</em> set. This mechanism is intended to assist with compatibility checks, and should be used with care in other situations.</p></blockquote><h4 id="runtime-compatibility-1" tabindex="-1">Runtime Compatibility <a class="header-anchor" href="#runtime-compatibility-1" aria-label="Permalink to &quot;Runtime Compatibility&quot;">​</a></h4><p>Use <code>acceptsProperty</code> to determine whether a snapshot recognizes a custom property name. Use <code>usesProperty</code> to learn if the snapshot includes references to the custom property name. Acceptance implies awareness of the set (and what it represents) even if selecting it will select no rules. Usage implies that selecting the set will select at least one rule.</p><p>The property <code>rule-set</code> is used to select a named set. A check of this property addresses whether the concept of a set is accepted or used by the snapshot.</p><blockquote><p>For improved compatibility checks, implementations are encouraged to reserve additional property names that enable checks for individual set names. For each set name, <em>foo</em>, the included implementations report property <code>rule-set:foo</code> as used.</p></blockquote><h4 id="implementation-1" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-1" aria-label="Permalink to &quot;Implementation&quot;">​</a></h4><p>The included implementations allow membership in any number of named sets as well as a default, unnamed set. Absent a contextual request for a named set, the unnamed set is selected by default. With a contextual set request, only the members of the requested set are selected.</p><blockquote><p>Unlike <em>value type</em> sets, the unnamed set is not always selected. Because of this difference, a <em>rule</em> may be assigned explicit membership in the unnamed set. This is optional for <em>rules</em> that belong only to the unnamed set and is required for <em>rules</em> meant to be selected as part of the unnamed set and one or more named sets.</p></blockquote><p><em>Example 2: rule set memberships</em></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>profile:</span></span>
<span class="line"><span>  valueType:</span></span>
<span class="line"><span>    name: type1</span></span>
<span class="line"><span>    rule:</span></span>
<span class="line"><span>      - name: foo</span></span>
<span class="line"><span>      - memberOf:</span></span>
<span class="line"><span>          - name: &#39;&#39;</span></span>
<span class="line"><span>      - memberOf:</span></span>
<span class="line"><span>          - name: &#39;&#39;</span></span>
<span class="line"><span>          - name: set1</span></span>
<span class="line"><span>      - memberOf:</span></span>
<span class="line"><span>          - name: set1</span></span>
<span class="line"><span>  property:</span></span>
<span class="line"><span>    name: &#39;rule-set:set2&#39;</span></span></code></pre></div><p>The rules described here are intentionally incomplete, showing only what is necessary for this example. Four rules are defined:</p><ol><li>The rule named <em>foo</em> implicitly belongs to the unnamed set.</li><li>The second rule explicitly belongs to the unnamed set.</li><li>The third rule explicitly belongs to the unnamed set and to <code>set1</code>.</li><li>The fourth rule belongs to <code>set1</code>.</li></ol><p>property <code>rule-set</code> is both accepted and used. Properties <code>rule-set:</code> and <code>rule-set:set1</code> are both accepted and used, intended for use with compatibility checks. Property <code>rule-set:set2</code> is accepted.</p><h2 id="operations" tabindex="-1">Operations <a class="header-anchor" href="#operations" aria-label="Permalink to &quot;Operations&quot;">​</a></h2><h3 id="maskvalue" tabindex="-1">maskValue <a class="header-anchor" href="#maskvalue" aria-label="Permalink to &quot;maskValue&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>bool maskValue(const char* valueType, const char* maskStyle, char* buffer, size_t offset, size_t length, bool conditionally) const;</span></span>
<span class="line"><span>bool maskValueConditionally(const char* valueType, const char* maskStyle, char* buffer, size_t offset, size_t length) const;</span></span>
<span class="line"><span>bool maskValueUnconditionally(const char* valueType, const char* maskStyle, char* buffer, size_t offset, size_t length) const;</span></span></code></pre></div><h4 id="overview-2" tabindex="-1">Overview <a class="header-anchor" href="#overview-2" aria-label="Permalink to &quot;Overview&quot;">​</a></h4><p>Given a single <em>domain</em> datum, obfuscate the content &quot;as needed&quot;. The framework anticipates two interpretations of &quot;as needed&quot;, either conditional or unconditional:</p><ul><li>Conditional means that values of a named type are only obfuscated if the named type is known by and selected in the snapshot. This usage assumes that the <em>profile</em> is the authoritative source for obfuscation requirements. The <em>profile</em>&#39;s consumer may ask for any value to be obfuscated, but the <em>profile</em> is not required to act.</li><li>Unconditional means that all value obfuscation requests will result in obfuscation. The named type is not required to be known by or selected in the snapshot. This usage assumes that the <em>profile</em>&#39;s consumer is the authoritative source for obfuscation requirements. If a consumer asks for a value to be obfuscated, the <em>profile</em> must act.</li></ul><p>Each <em>plugin</em> will define its own interpretation of &quot;as needed&quot;. The API distinction between conditional and unconditional is a hint intended to guide implementations capable of both, and should be ignored by implementations that are not.</p><p>The <code>buffer</code>, <code>offset</code>, and <code>length</code> parameters define what is assumed to be a single <em>domain</em> datum. That is, every character in the given character range is subject to obfuscation.</p><p>The <code>valueType</code> parameter determines if obfuscation is required, with <code>conditionally</code> controlling whether an unrecognized <code>valueType</code> is ignored (<em>true</em>) or forced to obfuscate (<em>false</em>).</p><p>The <code>maskStyle</code> parameter may be used to affect the nature of obfuscation to be applied. If the parameter names a defined <em>mask style</em>, that obfuscation format is applied. If the parameter does not name a define <em>mask style</em>, the <em>value type&#39;s</em> default obfuscation format is applied.</p><h4 id="runtime-compatibility-2" tabindex="-1">Runtime Compatibility <a class="header-anchor" href="#runtime-compatibility-2" aria-label="Permalink to &quot;Runtime Compatibility&quot;">​</a></h4><p>The <code>canMaskValue</code> method of <code>IDataMasker</code> can be used to establish whether a snapshot supports this operation. A result of <em>true</em> indicates that the plugin is capable of performing obfuscation in response to a request. Whether a combination of input parameters exists that results in obfuscation depends on the definition of the snapshot.</p><p>The <code>hasValueType</code> method of <code>IDataMaskerInspector</code> can be used to check if a given name is selected in the snapshot. The reserved name &quot;*&quot; may be used to detect unconditional obfuscation support when available in the snapshot. Names defined yet unselected in the snapshot are not directly detectable, but can be detected by comparing results of using multiple <em>context</em> configurations.</p><blockquote><p>The name &quot;*&quot; is reserved by the abstraction for compatibility checks.</p></blockquote><p>To confirm the availability of a specific <em>mask style</em> first requires confirmation of the <em>value type</em> to which the <em>mask style</em> is related. Use <code>queryValueType</code> to obtain the defined instance and, from the result, call <code>queryMaskStyle</code> to confirm the existence of the <em>mask stye</em>. For each of these, corresponding <code>get...</code> methods are defined to obtain new references to the objects. The methods are declared by <code>IDataMaskerInspector</code>.</p><h4 id="implementations" tabindex="-1">Implementations <a class="header-anchor" href="#implementations" aria-label="Permalink to &quot;Implementations&quot;">​</a></h4><p>The included implementations are inherently conditional. Obfuscation depends on matching <code>valueType</code> with a selected <em>value type</em> instance in the snapshot, where an instance is selected if it belongs to the currently selected <em>value type</em> set.</p><p>Unconditional obfuscation is enabled in profiles that include a <em>value type</em> instance named &quot;*&quot;. If an instance with this name is selected by the currently selected <em>value type</em> set, all values for undefined <em>value types</em> will be obfuscated. Values for defined but unselected <em>value types</em> will not be obfuscated.</p><ul><li>A value of type &quot;foo&quot; will be obfuscated when type &quot;foo&quot; is selected by the currently selected <em>value type</em> set.</li><li>A value of type &quot;foo&quot; will be obfuscated when type &quot;foo&quot; is undefined and type &quot;*&quot; is selected by the currently selected <em>value type</em> set.</li><li>A value of type &quot;foo&quot; will not be obfuscated when type &quot;foo&quot; is defined but unselected by the currently selected <em>value type</em> set.</li></ul><p><em>Example 3a: conditional obfuscation</em></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>valueType:</span></span>
<span class="line"><span>  - name: type1</span></span>
<span class="line"><span>  - name: type2</span></span>
<span class="line"><span>    memberOf:</span></span>
<span class="line"><span>      - name: set1</span></span></code></pre></div><p>This snippet declares two <em>value types</em>, with two total sets. The table shows which values will be conditionally obfuscated based on a the combination of <code>valueType</code> and the currently selected <em>value type</em> set.</p><table tabindex="0"><thead><tr><th style="text-align:center;"><code>valueType</code></th><th style="text-align:center;">Selected Set</th><th style="text-align:center;">Obfuscated</th></tr></thead><tbody><tr><td style="text-align:center;">type1</td><td style="text-align:center;">N/A</td><td style="text-align:center;">Yes</td></tr><tr><td style="text-align:center;">type1</td><td style="text-align:center;">set1</td><td style="text-align:center;">Yes</td></tr><tr><td style="text-align:center;">type2</td><td style="text-align:center;">N/A</td><td style="text-align:center;">No</td></tr><tr><td style="text-align:center;">type2</td><td style="text-align:center;">set1</td><td style="text-align:center;">Yes</td></tr><tr><td style="text-align:center;">typeN</td><td style="text-align:center;">N/A</td><td style="text-align:center;">No</td></tr><tr><td style="text-align:center;">typeN</td><td style="text-align:center;">set1</td><td style="text-align:center;">No</td></tr></tbody></table><blockquote><p>&quot;typeN&quot; in the table represents any named type, excluding the reserved &quot;*&quot;, not explicitly defined in the <em>profile</em>.</p></blockquote><p><em>Example 3b: unconditional obfuscation</em></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>valueType:</span></span>
<span class="line"><span>  - name: type1</span></span>
<span class="line"><span>  - name: type2</span></span>
<span class="line"><span>    memberOf:</span></span>
<span class="line"><span>      - name: set1</span></span>
<span class="line"><span>  - name: *</span></span></code></pre></div><p>Extending the previous example with a third <em>value type</em>, values of unknown type are now obfuscated. Values of known but unselected type remain unobfuscated.</p><table tabindex="0"><thead><tr><th style="text-align:center;"><code>valueType</code></th><th style="text-align:center;">Selected Set</th><th style="text-align:center;">Obfuscated</th></tr></thead><tbody><tr><td style="text-align:center;">type1</td><td style="text-align:center;">N/A</td><td style="text-align:center;">Yes</td></tr><tr><td style="text-align:center;">type1</td><td style="text-align:center;">set1</td><td style="text-align:center;">Yes</td></tr><tr><td style="text-align:center;">type2</td><td style="text-align:center;">N/A</td><td style="text-align:center;">No</td></tr><tr><td style="text-align:center;">type2</td><td style="text-align:center;">set1</td><td style="text-align:center;">Yes</td></tr><tr><td style="text-align:center;">typeN</td><td style="text-align:center;">N/A</td><td style="text-align:center;">Yes</td></tr><tr><td style="text-align:center;">typeN</td><td style="text-align:center;">set1</td><td style="text-align:center;">Yes</td></tr></tbody></table><blockquote><p>The name reserved by the abstraction to detect support for unconditional obfuscation is the same name reserved by the implementation to define that support.</p></blockquote><blockquote><p>&quot;typeN&quot; in the table represents any named type, excluding the reserved &quot;*&quot;, not explicitly defined in the <em>profile</em>.</p></blockquote><h3 id="maskcontent" tabindex="-1">maskContent <a class="header-anchor" href="#maskcontent" aria-label="Permalink to &quot;maskContent&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>bool maskContent(const char* contentType, char* buffer, size_t offset, size_t length) const;</span></span></code></pre></div><h4 id="overview-3" tabindex="-1">Overview <a class="header-anchor" href="#overview-3" aria-label="Permalink to &quot;Overview&quot;">​</a></h4><p>The <code>buffer</code>, <code>offset</code>, and <code>length</code> parameters define what is assumed to be a blob of content that may contain zero or more occurrences of <em>domain</em> data. The blob is expected to contain sufficient context allowing a snapshot&#39;s <em>rules</em> to locate any included occurrences.</p><p>The <code>context</code> parameter optionally influences which <em>rules</em> are selected in a snapshot, while the <code>contentType</code> parameter offers a hint as to the blob&#39;s data format. Each snapshot <em>rule</em> may be assigned a format to which it applies. Operation requests may be optimized by limiting the number of selected <em>rules</em> applied by describing the format. Selected <em>rules</em> not assigned a format will be applied in all requests.</p><blockquote><p>There is no equivalent to the unconditional mode offered by <code>maskValue</code>. Content obfuscation is always conditional on matching <em>rules</em>.</p></blockquote><h4 id="runtime-compatibility-3" tabindex="-1">Runtime Compatibility <a class="header-anchor" href="#runtime-compatibility-3" aria-label="Permalink to &quot;Runtime Compatibility&quot;">​</a></h4><p>The <code>canMaskContent</code> method of <code>IDataMasker</code> can be used to establish whether a snapshot supports this operation. A result of <em>true</em> indicates that the plugin is capable of performing obfuscation in response to a request. Whether a combination of input parameters exists that results in obfuscation depends on the definition of the snapshot.</p><p>The <code>hasRule</code> method of <code>IDataMaskerInspector</code> indicates if at least one <em>rule</em> is selected in the snapshot for a given content type. <em>Rules</em> not associated with any content type may be interrogated using an empty string.</p><blockquote><p>It is not possible to discern any information about the selected rules, such as their associated <em>value types</em> or the cues used to apply them in a buffer.</p></blockquote><h4 id="implementations-1" tabindex="-1">Implementations <a class="header-anchor" href="#implementations-1" aria-label="Permalink to &quot;Implementations&quot;">​</a></h4><p>The groundwork exists for two types of <em>rules</em>, serial and parallel. Serial rules, as the name implies, are evaluated sequentially. Parallel <em>rules</em>, on the other hand, are evaluated concurrently. Concurrent evaluation is expected to be more efficient than sequential, but no concurrent implementation is provided.</p><p>The included sequential implementation should be viewed as a starting point. Each <em>rule</em> defines a start token and an optional end token. For each occurrence of the start token in the blob, a corresponding search for an end token (newline if omitted), is performed. When both parts are found, the content between is obfuscated using the associated <em>value type&#39;s</em> default <em>mask style</em>.</p><blockquote><p>Traversing a potentially large blob of text once per <em>rule</em> is inefficient. A concurrent implementation is in development to improve performance and capabilities. A <em>domain</em> originated using the original implementation and migrated to the new implementation illustrates one <em>domain</em> implemented by multiple plugins and, by extension, multiple <em>profiles</em>.</p></blockquote><h3 id="maskmarkupvalue" tabindex="-1">maskMarkupValue <a class="header-anchor" href="#maskmarkupvalue" aria-label="Permalink to &quot;maskMarkupValue&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>bool maskMarkupValue(const char* element, const char* attribute, char* buffer, size_t offset, size_t length, IDataMaskingDependencyCallback&amp; callback) const;</span></span></code></pre></div><h4 id="overview-4" tabindex="-1">Overview <a class="header-anchor" href="#overview-4" aria-label="Permalink to &quot;Overview&quot;">​</a></h4><p>Where <code>maskValue</code> obfuscates individual values based on what the caller believes the value to be, and <code>maskContent</code> obfuscates any number of values it identifies based on cues found in surrounding text, <code>maskMarkupValue</code> obfuscastes individual values based on cues not provided to it.</p><p>Given a value and a relative location within a structure document, an implementation must decide whether obfuscation is required, may be required, or is not required. If required, it can obfuscate immediately. If not required, and can return immediately. If it might be required, the implementation must request the context it needs from the caller in order to make a final determination.</p><p>A request to mask the content of an element named <code>Value</code> might depend on the content of a sibling element named <code>Name</code>. If value of <code>Value</code> probably does not require obfuscation when the value of <code>Name</code> is <em>city</em>, but almost certainly does require obfuscation when the value of <code>Name</code> is <em>password</em>. When processing the request for <code>Value</code>, an implementation must ask for the value of a sibline named <code>Name</code> to decide whether or not to obfuscate the data.</p><blockquote><p>There is no equivalent to the unconditional mode offered by <code>maskValue</code>. Markup value obfuscation presumes that the caller is traversing a structured document without awareness of which values require obfuscation. If the caller knows which values require obfuscation, it should use <code>maskValue</code> on those specific values.</p></blockquote><h4 id="runtime-compatibility-4" tabindex="-1">Runtime Compatibility <a class="header-anchor" href="#runtime-compatibility-4" aria-label="Permalink to &quot;Runtime Compatibility&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    virtual bool getMarkupValueInfo(const char* element, const char* attribute, const char* buffer, size_t offset, size_t length, IDataMaskingDependencyCallback&amp; callback, DataMaskingMarkupValueInfo&amp; info) const = 0;</span></span></code></pre></div><p>The <code>canMaskMarkupValue</code> method of <code>IDataMasker</code> can be used to establish whether a snapshot supports this operation. A result of <em>true</em> indicates that the plugin is capable of performing obfuscation in response to a request. Whether a combination of input parameters exists that results in obfuscation depends on the definition of the snapshot.</p><p>The <code>getMarkupValueInfo</code> method of <code>IDataMaskerInspector</code> determines if the value of an element or attribute requires obfuscation. If required, the <code>info</code> parameter will describe on completion how to perform the obfuscation using <code>maskValue</code> instead of <code>maskMarkupValue</code>. This is done to optimize performance by eliminating the need to reevaluate rules to re-identify a match.</p><p>Assme a <em>value type</em> representing passwords is defined. If given an element value containing a URL with an embedded password, obfuscation is required. But most likely not for the entire value. In addition to identifying the <em>value type</em> associated with value, the offset and length of the embedded substring requiring obfuscation are supplied; use of <code>maskValue</code> with these three values and no <em>mask style</em> will apply the default obfuscation only to the embedded password. Alternatively, if a <em>mask style</em> is supplied, use of <code>maskValue</code> with this style and the original value offset and length should obfuscate only the embedded password.</p><blockquote><p>A <code>callback</code> interface is required for all calls to <code>getMarkupValueInfo</code>. The interface is only used when additional document context is required to make a determination about the requested value. In these cases, the checks are proximity-dependent and cannot be performed as part of load-time compatibility checks, when no document is available.</p></blockquote><h4 id="implementations-2" tabindex="-1">Implementations <a class="header-anchor" href="#implementations-2" aria-label="Permalink to &quot;Implementations&quot;">​</a></h4><p>TBD</p><h2 id="load-time-compatibility" tabindex="-1">Load-time Compatibility <a class="header-anchor" href="#load-time-compatibility" aria-label="Permalink to &quot;Load-time Compatibility&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>bool checkCompatibility(const IPTree&amp; requirements) const;</span></span></code></pre></div><h3 id="overview-5" tabindex="-1">Overview <a class="header-anchor" href="#overview-5" aria-label="Permalink to &quot;Overview&quot;">​</a></h3><p>Use of runtime compatibility checks may be unavoidable in some circumstances, but reliance on this in every script is inefficient. It may also devalue trace output by omitting messaging required to debug an issue, a problem that might only be found when said messaging is needed.</p><p>The <code>requirements</code> parameter represents a standardized set of runtime compatibility checks to be applied. By default, each check must pass to establish compatibility. Explicitly optional checks may be included to detect conditions the caller is prepared to work around.</p><blockquote><p>This can test which values will or won&#39;t be affected by <code>maskValue</code>, and which obfuscation styles are available. It can test which <em>rule</em> sets will impact <code>maskContent</code>. It specifically excludes <code>maskMarkupValue</code> checks that depend on the proximity of one value to another.</p></blockquote><p>Returning to the earlier SSN example, a caller might require that an SSN <em>value type</em> will be obfuscated. It might also want to use a particilar <em>mask style</em>, but may be prepared for its absence. A caller may prefer to mask the first four digits but may accept masking the last five instead, or may omit certain trace messages.</p><h3 id="implementation-2" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-2" aria-label="Permalink to &quot;Implementation&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>compatibility:</span></span>
<span class="line"><span>  - context:</span></span>
<span class="line"><span>      - domain: optional text</span></span>
<span class="line"><span>        version: optional number</span></span>
<span class="line"><span>        property:</span></span>
<span class="line"><span>          - name: required text</span></span>
<span class="line"><span>            value: required text</span></span>
<span class="line"><span>    accepts:</span></span>
<span class="line"><span>      - name: required text</span></span>
<span class="line"><span>        presence: one of &quot;r&quot;, &quot;o&quot;, or &quot;p&quot;</span></span>
<span class="line"><span>    uses:</span></span>
<span class="line"><span>      - name: required text</span></span>
<span class="line"><span>        presence: one of &quot;r&quot;, &quot;o&quot;, or &quot;p&quot;</span></span>
<span class="line"><span>    operation:</span></span>
<span class="line"><span>      - name: one of &quot;maskValue&quot;, &quot;maskContent&quot;, or &quot;maskMarkupValue&quot;</span></span>
<span class="line"><span>        presence: one of &quot;r&quot;, &quot;o&quot;, or &quot;p&quot;</span></span>
<span class="line"><span>    valueType</span></span>
<span class="line"><span>      - name: required text</span></span>
<span class="line"><span>        presence: one of &quot;r&quot;, &quot;o&quot;, or &quot;p&quot;</span></span>
<span class="line"><span>        maskStyle:</span></span>
<span class="line"><span>          - name: required text</span></span>
<span class="line"><span>            presence: one of &quot;r&quot;, &quot;o&quot;, or &quot;p&quot;</span></span>
<span class="line"><span>        Set:</span></span>
<span class="line"><span>          - name: required text</span></span>
<span class="line"><span>            presence: one of &quot;r&quot;, &quot;o&quot;, or &quot;p&quot;</span></span>
<span class="line"><span>    rule:</span></span>
<span class="line"><span>      - contentType: required text</span></span>
<span class="line"><span>        presence: one of &quot;r&quot;, &quot;o&quot;, or &quot;p&quot;</span></span></code></pre></div><p>The <code>requirements</code> parameter of <code>checkCompatibility</code> must be either a <code>compatibility</code> element or the parent of a collection of <code>compatibility</code> elements. Each instance may contain at most one <code>context</code> element, three <code>operation</code> elements (one per operation), and a variable number of <code>accepts</code>, <code>uses</code>, <code>valueType</code>, or <code>rule</code> elements.</p><p>The <code>context</code> element describes the target of an evaluation. It may include a <em>domain</em> identifier, or assume the default <em>domain</em>. It may include a version number, or assume the default snapshot of the <em>domain</em>. It may specify any number of custom <em>context</em> properties to select various profile elements in the snapshot.</p><p>The <code>accepts</code> and <code>uses</code> elements identify custom <em>context</em> property names either accepted or used within a snapshot. Acceptance indicates some part of a snapshot has declared an understanding of the named property. Usage indicates the some part of a snapshot will react to the named value being set. Usage implies acceptance.</p><blockquote><p>To improve compatibility check capabilities, a snapshot may synthesize properties that, if set, will have no effect. Specifically, a caller may be more interested to know if a particular set name (for either <em>value type</em> or <em>rule</em> set membership) is used than to know that an unidentified set name is used.</p></blockquote><p>The <code>Operations</code> element identifies which operations must be supported. Its purpose is to enforce availability of an operation when no more explicit requirements are given (for example, one may require <code>maskContent</code> without also requiring the presence of rules for any given content type). When more explicit requirements are given, the attributes of this element are redundant. If all attributes are redundant, the element may be omitted.</p><p>The <code>valueType</code> element describes all optional and mandatory requirements for using <code>maskValue</code> with a single <em>value type</em> name and optional <em>mask style</em> name. Set membership requirements can also be evaluated, which is most useful when <code>compatibility/context/property/@name</code> is &quot;*&quot;.</p><p>The <code>rule</code> element describes all optional and mandatory requirements for using <code>maskContent</code> with a single content type value. Unlike evaluable <em>value type</em> set membership, <em>rule</em> set membership requirements cannot be evaluated.</p><p>In all elements described as accepting <code>@presence</code>, the acceptable values are</p><ul><li><strong>r</strong>: the check is <em>required</em> to pass</li><li><strong>o</strong>: the check is <em>optional</em></li><li><strong>p</strong>: the check is <em>prohibited</em> from passing</li></ul>`,170)]))}const h=t(o,[["render",i]]);export{u as __pageData,h as default};
