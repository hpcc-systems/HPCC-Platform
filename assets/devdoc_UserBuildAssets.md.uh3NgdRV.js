import{_ as s,a,o as t,ag as n}from"./chunks/framework.Do1Zayaf.js";const o="/HPCC-Platform/assets/repository-tag-tab.DSfut5r1.png",i="/HPCC-Platform/assets/actions-secrets-and-variables.BfAcH5xn.png",r="/HPCC-Platform/assets/HPCC-12345-build-in-progress.DU0-PikH.png",f=JSON.parse('{"title":"Build Assets for individual developer","description":"","frontmatter":{},"headers":[],"relativePath":"devdoc/UserBuildAssets.md","filePath":"devdoc/UserBuildAssets.md","lastUpdated":1755191786000}'),l={name:"devdoc/UserBuildAssets.md"};function p(c,e,d,u,h,g){return t(),a("div",null,e[0]||(e[0]=[n('<h1 id="build-assets-for-individual-developer" tabindex="-1">Build Assets for individual developer <a class="header-anchor" href="#build-assets-for-individual-developer" aria-label="Permalink to &quot;Build Assets for individual developer&quot;">​</a></h1><h2 id="build-assets" tabindex="-1">Build Assets <a class="header-anchor" href="#build-assets" aria-label="Permalink to &quot;Build Assets&quot;">​</a></h2><p>The modern tool used for generating all our official assets is the Github Actions build-asset workflow on the hpcc-systems/HPCC-Platform repository, located <a href="https://github.com/hpcc-systems/HPCC-Platform/actions/workflows/build-assets.yml" target="_blank" rel="noreferrer">here</a>. Developers and contributors can utilize this same workflow on their own forked repository. This allows developers to quickly create assets for testing changes and test for errors before the peer review process.</p><p>Build assets will generate every available project under the HPCC-Platform namespace. There currently is not an option to control which packages in the build matrix get generated. But most packages get built in parallel, and <strong>released</strong> after the individual matrix job is completed, so there is no waiting on packages you don&#39;t need. Exceptions to this are for packages that require other builds to complete, such as the <strong>ECLIDE</strong>.</p><p>Upon completion of each step and matrix job in the workflow, the assets will be output to the repositories tags tab. An example for the <code>hpcc-systems</code> user repository is <a href="https://github.com/hpcc-systems/HPCC-Platform/tags" target="_blank" rel="noreferrer">hpcc-systems/HPCC-Platform/tags</a>.</p><p><img src="'+o+'" alt="Tag tab screenshot"></p><h2 id="dependent-variables" tabindex="-1">Dependent variables <a class="header-anchor" href="#dependent-variables" aria-label="Permalink to &quot;Dependent variables&quot;">​</a></h2><p>The build assets workflow requires several <strong>repository secrets</strong> be available on a developers machine in order to run properly. You can access these secrets and variables by going to the <code>settings</code> tab in your forked repository, and then clicking on the <code>Secrets and Variables - Actions</code> drop down under <code>Security</code> on the lefthand side of the settings screen.</p><p><img src="'+i+`" alt="Actions secrets and variables"></p><p>Create a secret by clicking the green <code>New Repository Secret</code> button. The following secrets are needed;</p><ul><li>LNB_ACTOR - Your Github username</li><li>LNB_TOKEN - Classic Github token for your user with LN repo access</li><li>DOCKER_USERNAME - Your docker.io username</li><li>DOCKER_PASSWORD - Your docker.io password</li><li>SIGNING_CERTIFICATE - pks12 self signed cert encoded to base64 for windows signing</li><li>SIGNING_CERTIFICATE_PASSPHRASE - passphrase for pks12 cert</li><li>SIGNING_SECRET - ssh-keygen private key for signing linux builds</li><li>SIGN_MODULES_KEYID - email used to generate key</li><li>SIGN_MODULES_PASSPHRASE - passphrase for private key</li></ul><h3 id="generating-the-windows-signing-certificate" tabindex="-1">Generating the windows signing certificate <a class="header-anchor" href="#generating-the-windows-signing-certificate" aria-label="Permalink to &quot;Generating the windows signing certificate&quot;">​</a></h3><p>To generate the self signed certificate for windows packages, you will need to do the following steps.</p><ol><li>Generate a root certificate authority</li></ol><p><code>openssl req -x509 -sha256 -days 365 -nodes -newkey rsa:2048 -subj &quot;/CN=example.com/C=US/L=Boca Raton&quot; -keyout rootCA.key -out rootCA.crt</code></p><ol start="2"><li>Create the server secret key</li></ol><p><code>openssl genrsa -out server.key 2048</code></p><ol start="3"><li>generate a csr.conf file</li></ol><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>cat &gt; csr.conf &lt;&lt;EOF</span></span>
<span class="line"><span>[ req ]</span></span>
<span class="line"><span>default_bits = 2048</span></span>
<span class="line"><span>prompt = no</span></span>
<span class="line"><span>default_md = sha256</span></span>
<span class="line"><span>req_extensions = req_ext</span></span>
<span class="line"><span>distinguished_name = dn</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[ dn ]</span></span>
<span class="line"><span>C = US</span></span>
<span class="line"><span>ST = Florida</span></span>
<span class="line"><span>L = Boca Raton</span></span>
<span class="line"><span>O = LexisNexis Risk</span></span>
<span class="line"><span>OU = HPCCSystems Development</span></span>
<span class="line"><span>CN = example.com</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[ req_ext ]</span></span>
<span class="line"><span>subjectAltName = @alt_names</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[ alt_names ]</span></span>
<span class="line"><span>DNS.1 = example.com</span></span>
<span class="line"><span>IP.1 = 127.0.0.1</span></span>
<span class="line"><span></span></span>
<span class="line"><span>EOF</span></span></code></pre></div><ol start="4"><li>Generate Cert Signing Request</li></ol><p><code>openssl req -new -key server.key -out server.csr -config csr.conf</code></p><ol start="5"><li>Create cert.conf file</li></ol><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>cat &gt; cert.conf &lt;&lt;EOF</span></span>
<span class="line"><span></span></span>
<span class="line"><span>authorityKeyIdentifier=keyid,issuer</span></span>
<span class="line"><span>basicConstraints=CA:FALSE</span></span>
<span class="line"><span>keyUsage = digitalSignature, nonRepudiation, keyEncipherment, dataEncipherment</span></span>
<span class="line"><span>subjectAltName = @alt_names</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[ alt_names ]</span></span>
<span class="line"><span>DNS.1 = example.com</span></span>
<span class="line"><span></span></span>
<span class="line"><span>EOF</span></span></code></pre></div><ol start="6"><li>Generate SSL cert with self signed CA</li></ol><p><code>openssl x509 -req -in server.csr -CA rootCA.crt -CAkey rootCA.key -CAcreateserial -out server.crt -days 365 -sha256 -extfile cert.conf</code></p><ol start="7"><li>Use server.crt to generate PKCS12 needed for windows tools</li></ol><p><code>openssl pkcs12 -inkey server.key -in server.crt -export -name &quot;hpcc_sign_cert&quot; -out hpcc_sign_cert.pfx</code></p><p>You will be asked to &quot;enter export password&quot;, this will be what goes in the variable SIGNING_CERTIFICATE_PASSPHRASE in Github Actions.</p><ol start="8"><li>Convert to base64</li></ol><p>On linux: <code>base64 hpcc_sign_cert.pfx &gt; hpcc_sign_cert.base64</code></p><p>On MacOS: <code>base64 -i hpcc_sign_cert.pfx -o hpcc_sign_cert.base64</code></p><p>From here you can <code>cat</code> the output of hpcc_sign_cert.base64 and copy the output into the variable SIGNING_CERTIFICATE in Github Actions.</p><h3 id="generating-a-signing-key-for-linux-builds" tabindex="-1">Generating a signing key for linux builds <a class="header-anchor" href="#generating-a-signing-key-for-linux-builds" aria-label="Permalink to &quot;Generating a signing key for linux builds&quot;">​</a></h3><p>For linux builds we&#39;re going to generate a private key using GnuPG (gpg).</p><p>Start the process by entering a terminal and run the command;<code>gpg --full-generate-key</code></p><p>You will be given several options in this process.</p><p>For type of key, select <code>RSA and RSA default</code>.</p><p>For keysize, enter <code>4096</code>.</p><p>For expiration date, select <code>0 = key does not expire</code>.</p><p>Input your real name.</p><p>Input your company email address.</p><p>For comment, input something like <code>Github actions key for signing linux builds</code>.</p><p>Then it will ask you to enter a passphrase for the key, and confirm the passphrase. Do not leave this blank.</p><p>A key should be output and entered into your gpg keychain. Now we need to export the key for use in the github actions secret.</p><p>To extract your key run <code>gpg --output private.pgp --armor --export-secret-key &lt;email-address-used&gt;</code>.</p><p>Now open private.pgp, copy all, and go to github actions secrets. Paste the output into the secret &quot;SIGNING_SECRET&quot;</p><h2 id="starting-a-build" tabindex="-1">Starting a build <a class="header-anchor" href="#starting-a-build" aria-label="Permalink to &quot;Starting a build&quot;">​</a></h2><p>The build-asset workflow is kicked off by a tag being pushed to the developers HPCC-Platform repository. Before we push the tag to our HPCC-Platform repository, we will want to have other tags in place if we want LN and ECLIDE builds to function correctly. Suggested tag patterns are <code>community_HPCC-12345-rc1</code> or <code>HPCC-12345-rc1</code>.</p><p>If you choose not to tag the LN and ECLIDE builds, the community builds will generate but errors will be thrown for any build utilizing the LN repository. ECLIDE will not even attempt a build unless you are also successfully building LN due to the dependency scheme we use. The &#39;Baremetal&#39; builds are designed to generate our clienttools targets for windows-2022 and macos-12 distributions. These jobs contain both the COMMUNITY and LN builds. If the LN build is not tagged, the COMMUNITY section of the job will run, and the assets will be uploaded, but the job will fail when it tries to build LN.</p><p>If you choose to precede your Jira number with <code>community_</code> then you must tag LN with <code>internal_</code> and ECLIDE with <code>eclide_</code>. Otherwise just use the Jira tag in all three repositories.</p><p>Once the LN and ECLIDE repository tags have been created and pushed with the same base branch that your work is based on for the HPCC-Platform, then you are free to push the HPCC-Platform tag which will initiate the build process.</p><p>The summary of the build-asset workflow can then be viewed for progress, and individual jobs can be selected to check build outputs. <img src="`+r+'" alt="Build Summary HPCC-12345"></p><h2 id="asset-output" tabindex="-1">Asset output <a class="header-anchor" href="#asset-output" aria-label="Permalink to &quot;Asset output&quot;">​</a></h2><p>Assets from the workflow will be released into the corresponding tag location, either in the HPCC-Platform repository for all community based builds, or the LN repository for any builds containing proprietary plugins. Simply browse to the releases or tag tab of your repository and select the tag name you just built. The assets will show up there as the build completes. An example of this on the hpcc-systems repository is <a href="https://github.com/hpcc-systems/HPCC-Platform/releases" target="_blank" rel="noreferrer">hpcc-systems/HPCC-Platform/releases</a>.</p>',54)]))}const m=s(l,[["render",p]]);export{f as __pageData,m as default};
