import{_ as t,a as i,o as a,ag as s}from"./chunks/framework.Do1Zayaf.js";const p=JSON.parse('{"title":"Index Representation in the HPCC Systems Platform","description":"","frontmatter":{},"headers":[],"relativePath":"devdoc/indexformats.md","filePath":"devdoc/indexformats.md","lastUpdated":1770295448000}'),o={name:"devdoc/indexformats.md"};function r(n,e,l,d,h,c){return a(),i("div",null,e[0]||(e[0]=[s('<h1 id="index-representation-in-the-hpcc-systems-platform" tabindex="-1">Index Representation in the HPCC Systems Platform <a class="header-anchor" href="#index-representation-in-the-hpcc-systems-platform" aria-label="Permalink to &quot;Index Representation in the HPCC Systems Platform&quot;">​</a></h1><p>The indexes used by the HPCC systems are based on B-trees. The B-trees are stored in fixed-size nodes (typically 8KB), and there are 3 main types of node:</p><ul><li>Branch nodes.<br> These are the top levels of the search index, used to find the leaf nodes containing the records. There may be any number of levels of branch nodes, but typical values range from 0 (a very small index) to 3 for a very large index. The branch nodes have references to the next nodes in the search tree, which consist of the keyed fields for the first entry in the node and the file offset.</li><li>Leaf nodes.<br> These contain multiple index records - both the keyed and payload fields.</li><li>Blob nodes<br> Each row in an index is limited to 32K uncompressed. If a larger payload is required, it is stored separately in one or more blob nodes.</li></ul><p>All these nodes are compressed (in different ways) when stored on disk. The compressed representation can be anything from 1× to 50× smaller; 5× is a typical compression ratio.</p><h2 id="factors-affecting-index-performance" tabindex="-1">Factors Affecting Index performance <a class="header-anchor" href="#factors-affecting-index-performance" aria-label="Permalink to &quot;Factors Affecting Index performance&quot;">​</a></h2><p>There are four main factors that affect the performance of the indexes:</p><h3 id="in-memory-size" tabindex="-1">In-memory size <a class="header-anchor" href="#in-memory-size" aria-label="Permalink to &quot;In-memory size&quot;">​</a></h3><p>When a node has been read from disk and is ready to be searched, how much memory does it occupy?</p><p>The system has a cache of branch and leaf nodes that have been read from disk. If nodes take up less space in main memory, then more nodes can be held in the leaf cache. That reduces the time spent reading from disk and decompressing.</p><h3 id="disk-size" tabindex="-1">Disk size <a class="header-anchor" href="#disk-size" aria-label="Permalink to &quot;Disk size&quot;">​</a></h3><h4 id="benefits" tabindex="-1">Benefits <a class="header-anchor" href="#benefits" aria-label="Permalink to &quot;Benefits&quot;">​</a></h4><p>The size on disk is interesting for a few reasons:</p><ul><li>Smaller files cost less to store on the cloud.<br> They also cost less to copy and create. On bare-metal systems, it reduces the likelihood of running out of disk space.</li><li>The size of Roxie clusters is often determined by the size of the data.<br> If the data shrinks, then the Roxie cluster could run on less expensive compute. For some single-node environments enabling all the data to fit on the local disk has significant cost and deployment advantages.</li></ul><p>See the further discussion under the compression ratio below.</p><h4 id="effect-on-performance" tabindex="-1">Effect on performance <a class="header-anchor" href="#effect-on-performance" aria-label="Permalink to &quot;Effect on performance&quot;">​</a></h4><p>The effect of disk size on performance is less clear.</p><p>If an index lookup reads entries from many consecutive leaf nodes then increased compression will reduce the number of reads. However, if a search only tends to read a few items from one node there will be no benefit.</p><p>If the reduced disk size has a corresponding increase in in-memory size for each node then it may have a negative effect, because the internal cache will be less effective. However, it will increase the effectiveness of the Linux page cache since more data will be held in the compressed pages.</p><p>The effect is highly dependent on the pattern of data access. If there is an exponential decay in the access frequency, then better compression will help because the pages in the Linux page cache will be used multiple times, avoiding disk reads. If the access pattern is uniform across the dataset then the Linux page cache will give little benefit, and higher compression will reduce the performance. Past evidence suggests the former.</p><h3 id="decompression-time-and-compression-ratio" tabindex="-1">Decompression time and compression ratio <a class="header-anchor" href="#decompression-time-and-compression-ratio" aria-label="Permalink to &quot;Decompression time and compression ratio&quot;">​</a></h3><p>Each time a node is read from disk it needs to be decompressed before it can be used. Here are some ballpark ideas of the relative performance of some different decompression algorithms (actual numbers for compression ratios vary significantly depending on the data being compressed):</p><table tabindex="0"><thead><tr><th>Algorithm</th><th>Decompression Time</th><th>Compression Ratio</th></tr></thead><tbody><tr><td>lzw</td><td>300 µs</td><td>5×</td></tr><tr><td>lz4</td><td>50 µs</td><td>4×</td></tr><tr><td>zstd</td><td>100 µs</td><td>7×</td></tr></tbody></table><h4 id="decompression-time" tabindex="-1">Decompression time <a class="header-anchor" href="#decompression-time" aria-label="Permalink to &quot;Decompression time&quot;">​</a></h4><p>Fast decompression will obviously reduce the processing time. This will improve the latency and the throughput, and will potentially allow the same workload to be performed on fewer Roxie nodes because of the reduced CPU load.</p><h4 id="compression-ratio" tabindex="-1">Compression ratio <a class="header-anchor" href="#compression-ratio" aria-label="Permalink to &quot;Compression ratio&quot;">​</a></h4><p>The interaction between decompression times and compression ratios is complex once you take the Linux page cache into account...</p><p>Linux has a page cache which uses any spare memory to cache previous reads from disk. Often when loading a node from disk it will already be in the page cache because it has previously been read. That typically reduces the read time from ~150 µs (NVMe/SSD) to &lt;10 µs.</p><p>The Linux page cache contains compressed nodes, while the Roxie internal node-caches contain decompressed pages. For the same amount of memory, more nodes can be kept in the Linux page cache than the internal cache. Increasing memory allocated to the page cache reduces the number of nodes that are actually read from disk - which is more significant the slower the disk.</p><p>The faster the decompression, the greater the benefit of the Linux page cache over the internal cache. When index formats are changed you will need to re-tune the system to find the best balance.</p><p>The compression ratio further complicates the picture. As noted above, a high compression ratio means that fewer nodes can be held in memory, so the internal cache is less effective. However, the Linux page cache will be more effective so it may be a net benefit.</p><p>Also see the notes above about disk size and performance.</p><p>There is one special case worth noting. If the size in memory is the same as the size on disk then it is worth making the in-memory cache as large as possible. (Inplace indexes with expansion on demand also provide this.) If this is the case, then the worst performance is when the internal cache and the Linux page cache are the same size - since they are likely to be storing the same data, half the memory will be wasted.</p><h3 id="search-time" tabindex="-1">Search time <a class="header-anchor" href="#search-time" aria-label="Permalink to &quot;Search time&quot;">​</a></h3><p>This has not currently been explored in detail. If the majority of operations do not require nodes to be loaded from disk then the search time will be critical. As soon as nodes need to be loaded from disk and decompressed those times are likely to dominate. An approach that is slower to search, but reduces the number of disk reads, is likely to outperform a representation that is quicker to search.</p><p>There is scope for profiling and optimizing the search code in the future.</p><h3 id="balancing-the-factors" tabindex="-1">Balancing the factors <a class="header-anchor" href="#balancing-the-factors" aria-label="Permalink to &quot;Balancing the factors&quot;">​</a></h3><p>The ideal solution would be a format that had very high compression, no time to decompress and the in-memory size matched the disk size. That is unattainable, so what is the relative significance of the different factors?</p><ol><li><p>Minimize disk size.<br> If cost is the ultimate priority, then minimizing the disk size is likely to provide the largest benefit - since that often defines the size of the compute required.</p></li><li><p>Minimize real disk reads.<br> Disk reads take much longer than anything else, so reducing them will improve latency. If the disk reads are completely offloaded from the CPU and it is possible to increase the number of concurrent reads, then reducing the number of disk reads may not significantly improve throughput. The number of disk reads is most likely to be reduced by reducing the disk size, and setting aside a large proportion of the memory for compressed nodes.</p></li><li><p>Reduce decompression time.<br> If the compressed nodes are stored in the Linux page cache, then reducing the decompression time will help when they are loaded into memory. Reducing this improves both latency and throughput.</p></li><li><p>Decrease the in-memory size.<br> This will reduce the number of times pages will need to be read from page-cache/disk and decompressed. This will improve both latency and throughput.</p></li></ol><p>It isn&#39;t clear which of (3) or (4) is the most significant - it is highly dependent on the internal cache sizes and the data access patterns - but I think (2) probably is most important. That has implications for the default compression.</p><h4 id="batch-v-interactive-systems" tabindex="-1">Batch v interactive systems <a class="header-anchor" href="#batch-v-interactive-systems" aria-label="Permalink to &quot;Batch v interactive systems&quot;">​</a></h4><p>Batch systems are most concerned with throughput, interactive systems are concerned with minimizing latency. This can lead to different priorities for the two systems.</p><ul><li>Anything that reduces the CPU load is likely to improve both the latency and the throughput.</li><li>Reducing IO times will have a large effect on latency. If sufficient operations can be executed in parallel it may not affect throughput.</li><li>Reducing IO operations similarly has a much larger effect on latency than throughput.</li></ul><h5 id="interactive" tabindex="-1">Interactive <a class="header-anchor" href="#interactive" aria-label="Permalink to &quot;Interactive&quot;">​</a></h5><p>For interactive systems, minimizing the disk reads is the priority - since that is the operation with the highest latency. That suggests a relatively large Linux page cache, and relatively small Roxie node caches will be optimal - since that will minimize the number of nodes that must be fetched from disk.</p><h5 id="batch" tabindex="-1">Batch <a class="header-anchor" href="#batch" aria-label="Permalink to &quot;Batch&quot;">​</a></h5><p>Reducing CPU load is the priority. It is possible that throughput is higher with a larger Roxie node cache - since it will reduce the time to decompress pages. That reduced decompression load will need to be balanced with extra CPU load reading pages from the Linux page cache or disk. Theoretically reducing the Linux page cache should only reduce latency rather than throughput.</p><h2 id="index-formats" tabindex="-1">Index formats <a class="header-anchor" href="#index-formats" aria-label="Permalink to &quot;Index formats&quot;">​</a></h2><p>What are the different index formats supported by the system? What are their advantages and disadvantages?</p><h3 id="legacy" tabindex="-1">Legacy <a class="header-anchor" href="#legacy" aria-label="Permalink to &quot;Legacy&quot;">​</a></h3><p>In memory:</p><ul><li>Nodes store all the expanded rows in a contiguous block of memory.</li></ul><p>On disk:</p><ul><li>Branch nodes<br> Use a very lightweight compression - compressing common leading strings. Very fast to decompress, but typically compress by a factor of 2.</li><li>Leaf nodes<br> Use our own implementation of LZW to compress the block of rows. Relatively slow to decompress, but provides fairly good compression.</li><li>Blob nodes<br> Use LZW compression.</li></ul><h3 id="inplace" tabindex="-1">Inplace <a class="header-anchor" href="#inplace" aria-label="Permalink to &quot;Inplace&quot;">​</a></h3><p>In memory:</p><ul><li>Branch nodes<br> Use a proprietary compression format that is essentially a serialized prefix tree. This compression format works well for sorted, related data. It does not compress unrelated data very well. No decompression needed.</li><li>Leaf nodes<br> The keyed portion uses the same compression format as the branch nodes. The payload is stored separately - all the uncompressed payload fields in a contiguous block of memory. This format also has an option to keep the payload compressed, and only expand it when it is actually required (i.e., a matching entry has been found).</li></ul><p>On disk:</p><ul><li>Branch nodes<br> Essentially identical to in memory. No time to decompress, compresses by a factor of ~5 if fields are related.</li><li>Leaf nodes<br> Compression algorithm is configurable. Currently defaults to LZ4HC3S - which is the high compression version of LZ4, with the compression level set to 3. It uses the streamed API to reduce the time to build the indexes. Very fast to decompress.</li><li>Blob nodes<br> Currently use LZW compression.</li></ul><p>Version 9.14.48 adds support for inplace:zstds, which further reduces the size of the indexes. It is likely that the reduced disk size will outweigh the extra decompression time, and the default for compressed(&#39;inplace&#39;) will be changed to compressed(&#39;inplace:zstds&#39;) in version 10.4.0.</p><h3 id="hybrid" tabindex="-1">Hybrid <a class="header-anchor" href="#hybrid" aria-label="Permalink to &quot;Hybrid&quot;">​</a></h3><p>This format uses the same representation as inplace compression for branch nodes, and an improved version of the legacy compression for leaf nodes.</p><ul><li>Branch nodes<br> Uses the same format as inplace. Pages can be searched while compressed.</li><li>Leaf nodes<br> The whole leaf node - key and payload - is compressed as a single block. The compression algorithm can be configured. (Defaults to zstds6.)</li><li>Blob nodes<br> Use ZStd compression.</li></ul><p>Initial results suggest that the on-disk sizes of hybrid indexes are notably smaller than the legacy format, and generally smaller than the current &quot;inplace&quot;. They are very similar size to inplace:zstds for those indexes where inplace compression works well, but also compress well even when inplace compression is not as effective.</p><p>The hybrid format is supported for reading in builds 9.14.48, 10.0.22 and 10.2.0. The indexes should be created in version 10.0.22 or later.</p><h3 id="further-ways-of-reducing-the-index-sizes" tabindex="-1">Further ways of reducing the index sizes <a class="header-anchor" href="#further-ways-of-reducing-the-index-sizes" aria-label="Permalink to &quot;Further ways of reducing the index sizes&quot;">​</a></h3><p>Some of the following reduce in-memory size, some on-disk and some both</p><ul><li>Special case common leading text for all entries in a leaf node.<br> HPCC-35477. This requires compressing the data twice - which significantly increases the time to create the indexes. Initial experiments suggest a further 1% saving. It would also reduce the in-memory sizes. The trade off does not currently seem worthwhile, so it is not considered a priority at the moment - the code to implement the compression changes (but not reading) is in branch ghalliday/issue35477.</li><li>Encode fixed size strings as variable length.<br> HPCC-35386. This also relies on support for single-byte prefixed variable length strings. Initial work suggests that it can have a noticeable effect on some indexes, and no effect on others. The changes are likely to be released as a new ECL syntax in 10.2.x.</li><li>Use zstd for blobs<br> Blobs are not very common, but this is likely to significantly cut the sizes of files that do use them.</li><li>Use dictionaries<br> This has the potential of increasing compression. No further work has been done yet.</li></ul><h2 id="current-recommendations-for-using-the-new-index-formats" tabindex="-1">Current Recommendations for using the new index formats <a class="header-anchor" href="#current-recommendations-for-using-the-new-index-formats" aria-label="Permalink to &quot;Current Recommendations for using the new index formats&quot;">​</a></h2><p>What are the recommendations for index formats? The first question is what is the priority - reducing costs or improving performance?</p><h3 id="reducing-costs" tabindex="-1">Reducing costs <a class="header-anchor" href="#reducing-costs" aria-label="Permalink to &quot;Reducing costs&quot;">​</a></h3><p>Almost all indexes should use <code>compress(&#39;hybrid&#39;)</code>. This is likely to give the smallest disk sizes, with good decompression performance. A relatively small subset of indexes will create smaller files by using <code>compress(&#39;inplace:zstds,blob(zstd)&#39;)</code>. (This will become the default for inplace indexes in a future version.) Good candidates for inplace compression are indexes with few index fields that are fairly densely populated (e.g. phone numbers).</p><h3 id="improving-performance" tabindex="-1">Improving performance <a class="header-anchor" href="#improving-performance" aria-label="Permalink to &quot;Improving performance&quot;">​</a></h3><p>It will require careful profiling to determine the best approach. The first approximation should be to follow the recommendations for reducing costs. Then explore the following:</p><ul><li>Using <code>hybrid:lz4shc</code> for indexes with many keyed fields and <code>inplace:lz4shc</code> for indexes with few (or very dense) keyed fields.</li></ul><p>This will trade increased disk space for reduced decompression time. If all files fit on local fast NVMe storage, and there is no opportunity to reduce costs by reducing the cluster size (e.g. the cluster is already CPU constrained), then using lz4hcs rather than zstds compression will reduce the CPU load. Whether that outweighs the increased time to read from disk will be data and query dependent.</p><h3 id="indexes-with-payload-fields-in-the-keyed-portion" tabindex="-1">Indexes with payload fields in the keyed portion <a class="header-anchor" href="#indexes-with-payload-fields-in-the-keyed-portion" aria-label="Permalink to &quot;Indexes with payload fields in the keyed portion&quot;">​</a></h3><p>There are other indexes where there are fields in the keyed portion that should be in the payload. This causes two problems:</p><ul><li>Branch nodes are larger than they need to be since they contain extra data.</li><li>The inplace keyed compression does not work well on unrelated data, leading to bigger inplace indexes.</li></ul><p>Moving the fields is likely to be a worthwhile exercise:</p><ul><li>It will reduce the size and number of the branch nodes - occasionally significantly.</li><li>It may marginally speed up searching on the indexes because fewer fields need to be compared when performing a keyed match.</li><li>It will have a notable effect on the size of inplace indexes, but relatively little on hybrid indexes.</li><li>It potentially allows the payload fields to be implicitly trimmed, possibly improving compression.</li></ul><h3 id="small-key-fields-and-large-complex-payload" tabindex="-1">Small key fields and large complex payload <a class="header-anchor" href="#small-key-fields-and-large-complex-payload" aria-label="Permalink to &quot;Small key fields and large complex payload&quot;">​</a></h3><p>There are some keys that have a very small keyed portion, and a very large payload. If the compression ratio for the leaf nodes is &gt;~20% (?) then the lz4hc3 compression may not be as good as lzw compression, leading to larger indexes. The best approach would be to compress with &#39;inplace:zstds&#39; instead. This will reduce the decompression time and the disk size.</p><h3 id="highly-compressible-payload" tabindex="-1">Highly compressible payload <a class="header-anchor" href="#highly-compressible-payload" aria-label="Permalink to &quot;Highly compressible payload&quot;">​</a></h3><p>There are some indexes that are pathologically compressible - e.g., leaves take up less than 5% of the size on disk. This can cause problems when they are read - because an 8KB page can expand to 400KB in memory. That can cause fragmentation problems with the memory manager and means the leaf cache can only hold a few entries. In general the benefits of improved compression are likely to outweigh these problems, so the compression is limited to 100x by default.</p><p>If anything can be done to reduce the size of the payload in memory that is likely to improve performance.</p><p>Solutions:</p><ul><li>Use variable size strings in the payload.<br> The most likely cause of extreme compression is large fixed-size string fields that are mainly blank or contain short strings. Where that is the case, it is better to trim the fields and store them as varstrings. This will significantly increase the effectiveness of the internal cache - because the uncompressed size will be much smaller. It will also reduce the decompression time.</li><li>Enhance the platform to automatically compress padded strings.<br> This feature is coming soon.</li></ul><h2 id="what-about-remote-file-storage" tabindex="-1">What about remote file storage? <a class="header-anchor" href="#what-about-remote-file-storage" aria-label="Permalink to &quot;What about remote file storage?&quot;">​</a></h2><p>For batch systems, the throughput should be very similar when retrieving data from remote storage rather than locally, provided the number of concurrent requests can be increased to balance the increased read time.</p><p>For interactive systems, the extra latency is problematic. Configuring the local NVMe cache should significantly reduce the average latency, and fewer remote requests may also reduce the maximum latency.</p><h2 id="current-state-of-development" tabindex="-1">Current State of Development <a class="header-anchor" href="#current-state-of-development" aria-label="Permalink to &quot;Current State of Development&quot;">​</a></h2><ul><li>Inplace<br> Implemented. Default changed to inplace:lz4hc3. Should consider changing the default to inplace:zstd. Expansion of inplace index payloads on demand (rather than at index load time) is implemented, but its performance has not yet been evaluated.</li><li>Hybrid<br> Implemented for reading in 9.14.48 and writing in 10.0.22 and later.</li><li>Trim compression<br> HPCC-35386. Work in progress - likely to be available in 10.2.x, with reading supported in 10.0.22 and later.</li><li>Support for moving keyed fields.<br> This should now be complete.</li><li>Zstd compression of blobs<br> Complete. Can be configured for inplace indexes using the blob(zstd) option, it is the default for hybrid.</li><li>Local NVMe file cache<br> Implemented</li><li>Explicitly managed page cache<br> HPCC-35577. Not yet implemented.</li></ul><h2 id="compression-options" tabindex="-1">Compression options <a class="header-anchor" href="#compression-options" aria-label="Permalink to &quot;Compression options&quot;">​</a></h2><p>The compression specification has the following format:</p><p><code>compress(&#39;&lt;format&gt;[:[compression[(compress-options)]|compression[(compress-options)],index-options|index-options]]&#39;)</code></p><p>Compression formats (for details see above):</p><ul><li>legacy - the format that has been historically used</li><li>inplace - keyed portion is searchable without decompression</li><li>hybrid - inplace branches, legacy-style leaf nodes</li></ul><p>compression:</p><ul><li>lzw - historical compression method. Not recommended.</li><li>lz4s - fast to decompress, does not compress as well as lzw.</li><li>lz4shc - equally fast to decompress, slower to compress but more compact</li><li>zstds - compresses very well, relatively fast to decompress.</li><li>zstds3 - alias for zstds(level=3)</li><li>zstds6 - alias for zstds(level=6)</li><li>zstds9 - alias for zstds(level=9)</li></ul><p>NOTE: lz4s, lz4shc and zstds are the streaming versions and have an &#39;s&#39; suffix. They should be used for the node compression.</p><p>index-options:</p><ul><li>blob(compression) - control the format that is used to compress blobs. Note the options should be lzw, lz4, lz4hc and zstd (without the &#39;s&#39; suffix).</li></ul><p>index-options for inplace indexes, many are only for developers</p><ul><li>recompress=bool - should the payload be recompressed to see if it reduces in size? (default false)</li><li>maxCompressionFactor=n - Do not compress payload more than a factor of n (default 100)</li><li>reuse=bool - reuse compressor (defaults to true)</li><li>compressThreshold=n - use uncompressed if compressed &gt; threshold% of uncompressed (default 95%)</li><li>uncompressed=bool - avoid compressing the payload</li></ul><p>compress-options: Passed directly to the compression algorithm</p><ul><li>level=n - Which lz4/zstd compression level should be used?</li></ul><p>Examples:</p><p><code>compress(&#39;hybrid:zstds(level=9),blob(lz4hc)&#39;)</code></p>',108)]))}const f=t(o,[["render",r]]);export{p as __pageData,f as default};
