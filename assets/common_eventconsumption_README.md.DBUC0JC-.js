import{_ as o,a as t,o as n,ag as a}from"./chunks/framework.Do1Zayaf.js";const m=JSON.parse('{"title":"Event Consumption","description":"","frontmatter":{},"headers":[],"relativePath":"common/eventconsumption/README.md","filePath":"common/eventconsumption/README.md","lastUpdated":1758815462000}'),i={name:"common/eventconsumption/README.md"};function d(s,e,r,c,l,p){return n(),t("div",null,e[0]||(e[0]=[a(`<h1 id="event-consumption" tabindex="-1">Event Consumption <a class="header-anchor" href="#event-consumption" aria-label="Permalink to &quot;Event Consumption&quot;">​</a></h1><h1 id="ieventvisitor" tabindex="-1">IEventVisitor <a class="header-anchor" href="#ieventvisitor" aria-label="Permalink to &quot;IEventVisitor&quot;">​</a></h1><p>The <code>readEvents</code> function declared in <code>jevent.hpp</code> follows a push-parsing model for parsing event files created by <code>EventRecorder</code>, declared in the same file. Given a file path and an instance of a visitor implementation, processing consists of these steps:</p><ul><li>The function notifies the visitor that processing of a new file is beginning. The visitor may abort processing at this time.</li><li>The function notifies the visitor of zero or more events. The visitor may abort processing during any of these visits.</li><li>The function notifies the visitor that processing is complete.</li></ul><h2 id="ieventvisitationlink" tabindex="-1">IEventVisitationLink <a class="header-anchor" href="#ieventvisitationlink" aria-label="Permalink to &quot;IEventVisitationLink&quot;">​</a></h2><p>An extension of <code>IEventVisitor</code>, implementations can be combined in a singly linked list of visitors. Each node in the list chooses whether to propagate notification to the next node in the list. When propagating, each node controls what data is passed on.</p><h3 id="ieventfilter" tabindex="-1">IEventFilter <a class="header-anchor" href="#ieventfilter" aria-label="Permalink to &quot;IEventFilter&quot;">​</a></h3><p>An extension of <code>IEventVisitationLink</code>, this abstraction describes visitors whose purpose is to block unwanted events from propagating to the next visitor in the chain. Implementations define the filtering logic. Generally, an implementation will only prevent individual event visitations from propagating.</p><h4 id="ceventfilter" tabindex="-1">CEventFilter <a class="header-anchor" href="#ceventfilter" aria-label="Permalink to &quot;CEventFilter&quot;">​</a></h4><h3 id="ieventmodel" tabindex="-1">IEventModel <a class="header-anchor" href="#ieventmodel" aria-label="Permalink to &quot;IEventModel&quot;">​</a></h3><p>An extension of <code>IEventVisitationLink</code>, this abstraction describes visitors whose purpose is to more broadly transform the event stream. Possible changes include:</p><ul><li>event suppression, as with a filter (e.g., an <code>IndexEviction</code> might be discarded); or</li><li>attribute updates (e.g., an <code>ExpandTime</code> may be replaced with an estimate assuming a different decompression algorithm); or</li><li>event reclassification (e.g., an <code>IndexCacheHit</code> may become an <code>IndexCacheMiss</code>); or</li><li>event creation (e.g., an <code>IndexLoad</code> may be required after reclassifying <code>IndexCacheHit</code> to <code>IndexCacheMiss</code>).</li></ul><h4 id="cindexeventmodel" tabindex="-1">CIndexEventModel <a class="header-anchor" href="#cindexeventmodel" aria-label="Permalink to &quot;CIndexEventModel&quot;">​</a></h4><p>A concrete implementation of <code>IEventModel</code> that acts on events in the <code>EventCtxIndex</code> event context. All other events are propagated without change.</p><p>The options for stream transformation are extensive.</p><ul><li><code>ReadTime</code> values are replaced with estimates defined for alternate storage planes. Branch and leaf nodes for each file may be assigned a storage plane. <em>This is currently mandatory, but is so only because it was the first transformation to be implemented. Should it remain mandatory, or should it become optional like other transformations?</em></li><li><code>ReadTime</code> values may be replaced instead by an estimate assuming the use of a page cache.</li><li><code>InMemorySize</code> and <code>ExpandTime</code> values may be replaced with estimates as if a different compression algorithm had been used in the original event. One set of estimates per index node kind is supported, so branch and leaf nodes can be compressed with different algorithms.</li><li><code>InMemorySize</code> and <code>ExpandTime</code> values may be changed to simulate different decompression strategies. For events recorded when node decompression is reported with <code>IndexLoad</code> events, the effects of decompression may be deferred to <code>IndexPayload</code> events. This results in reduced node cache size and avoids unnecessary decompression, but with the risk of repeated decompressions. This can be enabled separately for branch and leaf nodes.</li><li>The effects of changing the node cache size can be simulated, with different limits for branch and leaf nodes. This option radically alters the event stream: <ul><li>An <code>IndexCacheHit</code> event may be replaced by an <code>IndexCacheMiss</code>, <code>IndexLoad</code>, and zero or more <code>IndexEviction</code> events.</li><li>An <code>IndexCacheMiss</code> event may be replaced by an <code>IndexCacheHit</code> event. The next <code>IndexLoad</code> event for the same node will then be suppressed.</li><li>And <code>IndexLoad</code> event may be followed by one or more <code>IndexEviction</code> events.</li><li>An input <code>IndexEviction</code> event is always suppressed.</li></ul></li></ul><p>Some events may be discarded by the model if they are observed when unexpected. Modeling and <em>on-demand</em> node expansion from <em>on-load</em> events and custom node cache sizes imposes a need for information made available in <code>IndexCacheHit</code> and <code>IndexLoad</code> events. <code>IndexLoad</code> events are expected to follow <code>IndexCacheMiss</code> events. <code>IndexLoad</code> events not preceded by either a cache hit or miss for the same node are discarded. <code>IndexPayload</code> and <code>IndexEviction</code> events not preceded by either a cache hit or the combination of a cache miss and load for the same node are discarded.</p><p>Model options are specified using a property tree. The configuration conforms to this format:</p><pre><code>@kind
</code></pre><p>An optional identifier that must be <code>index-events</code> when present.</p><pre><code>storage/
</code></pre><p>Required container for storage plane and page cache configuration settings.</p><pre><code>storage/
    @cacheReadTime
    @cacheCapacity
</code></pre><p>Optional page cache controls. Setting a positive read time enables the page cache. Setting a positive capacity limits the total size of cached pages. Note that unlike a real page cache, which would cache the entire page, the model merely tracks which 8 KB pages are cached based on file ID and offset.</p><p><strong>If not zero, the capacity must be at least 8 KB. It is an error to configure the page cache such that it cannot cache at least one page.</strong></p><pre><code>storage/
    plane/
</code></pre><p>Required and repeatable element container for a single storage plane. The first occurrence becomes the default plane for any file that is not explicitly assigned a plane.</p><pre><code>storage/
    plane/
        @name
</code></pre><p>Required unique identifier through which files identify the plane or planes they use.</p><pre><code>storage/
    plane/
        @readTime
</code></pre><p>Required number of nanoseconds needed to load a single 8KB page from the storage device.</p><pre><code>storage/
    file/
</code></pre><p>Optional repeatable element enabling one or all files to be explicitly associated with a storage plane.</p><pre><code>storage/
    file/
        @path
</code></pre><p>Optional unique file path identifying the physical file being configured. When present, the value must match the value presented in the<code>Path</code> attribute of a <code>FileInformation</code> meta event. Omission, or empty, applies this configuration to all files not explicitly configured in a sibling element.</p><pre><code>storage/
    file/
        @plane
</code></pre><p>Optional storage plane name identifies the storage device on which the file is stored. When present, the value must match one of the configured storage plane names. Omission, or empty, associates the file or files to the default plane</p><pre><code>storage/
    file/
        @branchPlane
        @leafPlane
</code></pre><p>Optional storage plane names associating all nodes of a given kind to a storage plane. When present, the value must match one of the configured storage plane names. Omission, or empty, with the default plane for the file.</p><pre><code>memory/
</code></pre><p>Optional container for decompression performance estimates, decompression strategies, and node cache settings.</p><pre><code>memory/
    node/
</code></pre><p>The optional configuration of memory expansion modeling for each index node kind:</p><ul><li><p>Expansion mode modeling may override the default <code>expansionMode</code> by including a <code>expansionMode</code> property. The default is inherited from the parent element.</p></li><li><p>Expansion estimation must include both <code>sizeFactor</code> and <code>sizeToTimeFactor</code> properties. No defaults are defined. These properties must be specified for every node kind to enable estimation.</p></li><li><p>Node cache size control must include a <code>cacheCapacity</code> property.</p><p>memory/ node/ @kind</p></li></ul><p>A value of zero for branch nodes or one for leaf nodes. Required to specify a leaf node, and optional to specify a branch node. A <code>node</code> without <code>kind</code> is ignored.</p><pre><code>memory/
    node/
        @sizeFactor
        @sizeToTimeFactor
</code></pre><p>Optional controls for decompression performance estimates for the desginated node kind. To enable compression estimates, both values must be specified for all node types. Both values must be omitted for all node types to proceed without estimations. Every other combination of these values is an error.</p><p>Both values are positive decimal (i.e., floating point) numbers. The estimated <code>InMemorySize</code> for a decompressed node is the product of the size factor and 8 KB. The estimated <code>ExpandTime</code> for the same node is the product of the estimated <code>InMemorySize</code> and the size to time factor.</p><pre><code>memory/
    node/
        @expansionMode
</code></pre><p>Optional choice of:</p><ul><li><code>ll</code>: The model input and output are both assumed to represent node expansion on-load. This is the default.</li><li><code>ld</code>: The model input is assumed to represent node expansion on-load, and the model output represents node-expansion on-demand.</li><li><code>dd</code>: The model input and output are both assumed to represent node expansion on-demand.</li></ul><p>This does not apply to branch nodes, which always use <em>on-load</em> expansion.</p><pre><code>memory/
    node/
        @cacheCapacity
</code></pre><p>Optional upper limit, in bytes, for a node cache for nodes of the desginated kind. Omission, empty, or zero, prevents the model from caching. Note the discrepancy between this capacity and the page cache capacity reflects the page cache&#39;s requirement for a separately configured time duration. Note that unlike a real node cache, which would cache the entire page, the model caches the size, in bytes, of the node as it is stored in memory.</p><p><strong>If not zero, the capacity must be at least the size of the largest node size. It is an error to configure the page cache such that it cannot cache at least one page, but the error will not be detected until an offending node is observed.</strong></p><pre><code>memory/
    observed/
</code></pre><p>Optional and repeatable element to be used by unit tests to pre-populate the memory model&#39;s internal data. Its use simplifies the establishment of cache preconditions when evaluating the effects of the model&#39;s cache on the event stream.</p><p>Each instance represents one loaded page. The data has value for both alternate decompression strategies and node caching.</p><p>It is worth noting that element attributes follow the title case naming convention instead of the camel case used in the rest of the configuration. This is intentional, as the names match the event attribute names from which the model would normally be updated.</p><pre><code>memory/
    observed/
        @NodeKind
        @FileId
        @FileOffset
</code></pre><p>These identify the node and the cache to which it belongs. The node may be the same as or differ from nodes referenced by input events.</p><pre><code>memory/
    observed/
        @ExpandTime
</code></pre><p>The number of nanoseconds required to expand the node one time. For nodes also referenced in input events, this value may differ from what is used in those events. The model may always replace the value with an estimated value. If not replaced, the model accepts that decompression times can vary based on system load and always keeps the last observed time.</p><pre><code>memory/
    observed/
        @InMemorySize
</code></pre><p>If algorithm estimation is enabled, the value is replaced. If the node will not be referenced by model input events, the value can be any positive integral value. Otherwise, the value must match the first non-zero <code>InMemorySize</code> value observed in an input event. Node sizes are not expected to change between observations, and the model does not account for unexpected changes. Once a node is in the cache with size <code>X</code>, it must always have size <code>X</code>.</p>`,65)]))}const f=o(i,[["render",d]]);export{m as __pageData,f as default};
