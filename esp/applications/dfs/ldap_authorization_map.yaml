ldap:
  root_access:
    resource: WsDfsAccess
    required: Read
    description: Base access to EclWatch
  resource_map:
    ws_dfsservice:
      Feature:
      - path: DfsAccess
        resource: DfsAccess
        description: Base access to DFS services