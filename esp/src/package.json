{
  "name": "eclwatch",
  "version": "10.0.0",
  "description": "ECL Watch Mono Repo",
  "workspaces": [
    "packages/legacy"
  ],
  "scripts": {
    "clean": "lerna run clean",
    "build": "lerna run build",
    "build-dev": "lerna run build-dev",
    "test": "lerna run test"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "lerna": "8.1.8"
  }
}